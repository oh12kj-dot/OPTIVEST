# Handoff — PHASE0-GIT-IDENTITY-V2 independently accepted; commit/push not performed

Updated: 2026-09-21 (Asia/Tokyo). Branch main; HEAD UNBORN; no commits or remote.

## 2026-09-21 final alternate-token Sol recheck — PASS_ENGINEERING_PHASE0_GIT_IDENTITY

- Controlling complete post-review SHA-256: `71CD35864C4B0FE33271458250FC920FCD3918453E85A78133F22A7A8C60C2BE`.
- Reviewed verifier/test hashes: `D411F2043716C08302170C962A480CD6BE3C86D1632FCEA96308C6AF9DEECF45` / `641759C3FBA8E7936E69D119F33B8CDC9E03AB6652EADB6BCDF6486504D43953`; Policy/frozen rev2/rev3/rev4/pre-review/ADR identities unchanged.
- Independent evidence: genuine malformed committed token plus malformed tree; six blob-token variants across tree/collision/invalid START; candidate/blob trigger accounting; genuine missing/executable/symlink/gitlink collision pairs; old command/stderr blockers; B1/B2/F1/F2. All exact findings/null semantics, non-leak and snapshots passed.
- Focused **29 passed, 1 skipped** in **138.597s**. Terra full evidence **228 passed, 2 skipped, 2 warnings, 174 subtests**. Compile/startup/diff/trailing checks pass.
- Verdict: `PASS_ENGINEERING_PHASE0_GIT_IDENTITY`. Initial commit/push is technically unblocked by this gate but was not performed. Repository remains `main / HEAD UNBORN`, no remote.
- First next action: supervisor decides/executes the already-scoped initial Git delivery workflow or resumes the next authorized task; do not infer Production, model, provider, risk or approval status from this PASS.
- Boundaries: `RISK BUDGET NOT APPROVED`, applicable evidence `NOT VERIFIED`, `SHADOW VALIDATION NOT PASSED`, `NOT PRODUCTION READY`.

## 2026-09-21 final committed-token precedence correction — Terra evidence complete

- Authority unchanged: Policy, ADR-0019/D0011, frozen rev2/rev3/rev4 and controlling prior review `5F01878CCF140811EA7723EFB1E175D0A24558391314250E88BC9A1346F2B440`.
- Changed only `scripts/verify_startup.py`, `tests/test_verify_startup.py` and Phase-0 operational records. After command validity and candidate-token syntax, a successful committed blob is decoded/validated as `GIT_TOKEN` before tree/collision/START; valid committed-token fields are populated before those later semantic rows. Failed blob reads preserve row-1 command behavior for valid/unparseable states and collision-then-START structural behavior for parsed invalid START entries.
- Permanent evidence adds: an actual committed malformed START blob plus independently malformed tree exact-report/snapshot probe; six malformed committed-blob token variants (missing, duplicate, BOM, CR, NUL, non-UTF8) against tree/collision/START; and explicit candidate-versus-blob trigger accounting in the pair helper so a proxy cannot mask the alternate route. The 12-row/66-pair exact matrix and collision/START fixture remain retained.
- Current hashes: verifier `D411F2043716C08302170C962A480CD6BE3C86D1632FCEA96308C6AF9DEECF45`; tests `641759C3FBA8E7936E69D119F33B8CDC9E03AB6652EADB6BCDF6486504D43953`.
- Verification: focused `uv --cache-dir .uv-cache run python -m unittest tests.test_verify_startup -v` -> **29 passed, 1 skipped** in **137.277s**; full `uv --cache-dir .uv-cache run pytest -q --tb=line` -> **228 passed, 2 skipped, 2 warnings, 174 subtests passed** in **244.28s**; compileall, structural startup, `git diff --check` and explicit trailing-whitespace scan exit 0. Startup is structural PASS on `main/UNBORN`, 78 untracked paths and `NOT PRODUCTION READY`.
- First next action: final independent Sol falsification against ADR-0019/rev4 and the controlling review. No engineering PASS, initial commit or push until that review.
- Boundaries unchanged: structural scope only; `RISK BUDGET NOT APPROVED`, applicable evidence `NOT VERIFIED`, `SHADOW VALIDATION NOT PASSED`, `NOT PRODUCTION READY`.

## 2026-09-21 final residual Sol recheck — REVIEW_REQUIRED

- Controlling complete post-review SHA-256: `5F01878CCF140811EA7723EFB1E175D0A24558391314250E88BC9A1346F2B440`.
- Reviewed verifier/test hashes: `0ED13A57DA47F4D20FB0656C291CB531B210CFD2721CE3A4DB294D4D019E3536` / `4FB0EFA8579D85FF2B3A17D33A43A23EBE83DC31129E9D30C68DFB7F9D1CBA26`; Policy/frozen triplet/pre-review/ADR unchanged.
- Closed: genuine Case/case collision plus missing, executable, symlink or gitlink START returns sole exact `GIT_CASE_COLLISION`, with null later fields and unchanged snapshot. Pair helper now composes tree faults and asserts every named injection was applied.
- New blocker: malformed committed START blob token plus malformed tree returns later `GIT_TREE`; rev4 requires earlier `GIT_TOKEN`. The helper models `GIT_TOKEN` only through the worktree candidate and therefore misses this triggering path.
- Focused **27 passed, 1 skipped** in **124.311s**; independent old-blocker/B1/B2/F1/F2 probes remain positive; compile/startup/diff/trailing checks pass. Terra full evidence **226 passed, 2 skipped, 156 subtests** was not rerun by Sol because the counterexample is dispositive.
- First next action: preserve collision-before-START, restore malformed committed-token-before-tree ordering, add the genuine combined fixture and exact report, then return to Sol. No engineering PASS, commit or push.
- Boundaries unchanged: `RISK BUDGET NOT APPROVED`, applicable evidence `NOT VERIFIED`, `SHADOW VALIDATION NOT PASSED`, `NOT PRODUCTION READY`.

## 2026-09-21 final collision/START correction — Terra evidence complete

- Authority unchanged: Policy and ADR-0019/D0011; controlling prior final Sol review `DA717DC223D70CDD9DF05CBCDBCE5AABDD29F7C0CC17A029C02B8D50BF2ABCFC`; frozen rev2/rev3/rev4/pre-review hashes remain pinned.
- Changed only `scripts/verify_startup.py`, `tests/test_verify_startup.py` and Phase-0 operational records. After token/tree validation, a collision now precedes START-entry validity; valid START blob parsing still establishes recorded fields for a collision-only row, while an invalid START collision retains null recorded fields.
- Test builder now composes all same-tree mutations: the direct fixture contains `Case.txt`, `case.txt` and a `100755 blob .ai/START_HERE.md`; it returns exact sole `GIT_CASE_COLLISION`, complete report and an unchanged full worktree/`.git` snapshot. The 12-row/66-pair helper tracks each advertised injection and fails when any fault is not actually applied, including raw combined tree faults.
- Current hashes: verifier `0ED13A57DA47F4D20FB0656C291CB531B210CFD2721CE3A4DB294D4D019E3536`; tests `4FB0EFA8579D85FF2B3A17D33A43A23EBE83DC31129E9D30C68DFB7F9D1CBA26`.
- Verification: focused `uv --cache-dir .uv-cache run python -m unittest tests.test_verify_startup -v` -> **27 passed, 1 skipped** in **124.291s**; full `uv --cache-dir .uv-cache run pytest -q --tb=line` -> **226 passed, 2 skipped, 2 warnings, 156 subtests passed** in **231.01s**; compileall, structural startup, `git diff --check` and explicit trailing-whitespace scan all exit 0. Startup is structural PASS on `main/UNBORN`, 78 untracked paths and `NOT PRODUCTION READY`.
- First next action: Sol independently falsifies this final narrow correction against ADR-0019/rev4 and the current review. No engineering PASS, initial commit or push until that review.
- Boundaries unchanged: structural scope only; `RISK BUDGET NOT APPROVED`, applicable evidence `NOT VERIFIED`, `SHADOW VALIDATION NOT PASSED`, `NOT PRODUCTION READY`.

## 2026-09-21 final independent Sol recheck — REVIEW_REQUIRED

- Controlling complete post-review SHA-256: `DA717DC223D70CDD9DF05CBCDBCE5AABDD29F7C0CC17A029C02B8D50BF2ABCFC`.
- Current verifier/test hashes: `AB3CC7A7EEB278AD7B02A58733BFD7E2DC83897329345B754B65FB1C5B57995C` / `87209E30F55EABDAC372ACA2F67E61FC84069FD355F1DCE63D5A528111B16DF7`; Policy and frozen rev2/rev3/rev4/pre-review/ADR identities remain unchanged.
- Prior blockers close independently: committed `HEAD` exit-zero/nonzero stderr, failed `cat-file` plus invalid token and later whole-status failure versus root/object/branch all return exact non-leaking `GIT_COMMAND`; actual missing/executable START entries return `GIT_START_ENTRY`; snapshots remain unchanged.
- Residual blocker: a genuine tree containing both ASCII case-colliding paths and executable START_HERE returns `GIT_START_ENTRY`; rev4 requires earlier `GIT_CASE_COLLISION`. The verifier checks START validity before collision.
- Permanent-matrix gap: the 12 individual exact reports are real and the test enumerates all 66 pair names, but its helper returns the collision rewrite before applying the START-mode rewrite. That one pair is not genuinely combined. Independent genuine-pair reproduction passed 65/66 and exposed this mismatch.
- Evidence: focused **26 passed, 1 skipped** in **122.406s**; independent probe closed prior blockers/B1/B2/F1/F2 and found the one pair mismatch; compile/startup/whitespace exit 0. Startup is structural PASS on `main/UNBORN`, 78 untracked paths, `NOT PRODUCTION READY`. Full suite was not rerun because the direct frozen-contract counterexample is dispositive; prior Terra full evidence remains **225 passed, 2 skipped, 156 subtests**.
- First next action: Terra changes only semantic check order and the combined collision/START test fixture, preserving all other behavior, then reruns the matrix and returns to Sol. No engineering PASS, commit or push.
- Boundaries unchanged: structural scope only; `RISK BUDGET NOT APPROVED`, applicable evidence `NOT VERIFIED`, `SHADOW VALIDATION NOT PASSED`, `NOT PRODUCTION READY`.

## 2026-09-21 residual B3/B4/F3 correction — Terra evidence complete

- Authority unchanged: Policy `ACF013CE46F450423D83DEEBA1C207B22C669670C2B9BDC83A8A665F5AD38E67`; ADR-0019/D0011; frozen rev2/rev3/rev4 and controlling prior recheck `FE613453AE10D5EFC2081A29C6CA98569DAEF78F8BD4F8991E956D3A05A44ABC`.
- Changed only `scripts/verify_startup.py` and `tests/test_verify_startup.py`, plus these operational records. Committed mode now gathers all state Git reads before root/object/branch semantics so any later command fault remains table-first `GIT_COMMAND`; the valid-tree missing/nonregular START exception remains `GIT_START_ENTRY`.
- Permanent real disposable-repository evidence: exact complete top-level JSON (including all observed null/value/readiness fields) for every rev4 row; all 66 ordered earlier/later pairs; byte snapshots of worktree plus all `.git` files around each run; no source-output leakage; exit-zero and nonzero stderr across committed and UNBORN applicable reads; malformed/truncated binary streams; actual missing and executable START entries; digest/collision/hidden-flag coverage.
- Current file hashes: verifier `AB3CC7A7EEB278AD7B02A58733BFD7E2DC83897329345B754B65FB1C5B57995C`; tests `87209E30F55EABDAC372ACA2F67E61FC84069FD355F1DCE63D5A528111B16DF7`.
- Verification: focused `uv --cache-dir .uv-cache run python -m unittest tests.test_verify_startup -v` -> **26 passed, 1 skipped** in **142.207s**; full `uv --cache-dir .uv-cache run pytest -q --tb=line` -> **225 passed, 2 skipped, 2 warnings, 156 subtests passed** in **258.53s**; compileall, structural startup and whitespace checks exit 0. Startup is structural PASS on `main/UNBORN`, 77 untracked paths, `NOT PRODUCTION READY`.
- First next action: Sol independently falsifies this exact diff against ADR-0019/rev4 and the controlling review. Do not issue `PASS_ENGINEERING_PHASE0_GIT_IDENTITY`, commit or push before that recheck.
- Boundaries unchanged: structural consistency only; `RISK BUDGET NOT APPROVED`, applicable evidence `NOT VERIFIED`, `SHADOW VALIDATION NOT PASSED`, `NOT PRODUCTION READY`.

## 2026-09-21 independent Sol recheck after bounded review-fix — REVIEW_REQUIRED

- Controlling review: `docs/reviews/phase0_git_identity_post_review.md`, complete SHA-256 `FE613453AE10D5EFC2081A29C6CA98569DAEF78F8BD4F8991E956D3A05A44ABC`; prior identity `6EFE50E741928633061D2D963235C111FC8FE94185907675F151849E4550B9B3` remains recorded inside it.
- Reviewed correction identities: verifier `A57762B8B9E76F17F013711612A151F37B6AF3F50AE8FA65EF5C31565C6FAB2B`; tests `773C7F18278F64C2F44F2E1D135C52657B18C3CA8B03CAA79447FCB1B17E170E`.
- Fixed: prior failed-`cat-file` plus invalid-token pair now emits exact `GIT_COMMAND`; added state-path stderr fixtures fail closed, do not leak and preserve snapshots.
- Remaining R1/B3/F3: on committed HEAD, stderr/nonzero from the separate `rev-parse --verify --quiet HEAD` read is ignored whenever `HEAD^{commit}` succeeds; independent exit-0 and exit-1 stderr probes both returned no finding.
- Remaining R2/B3/F3: an actual commit with START_HERE absent returns `GIT_COMMAND` from eager `cat-file`; frozen revision 4 requires `GIT_START_ENTRY` for a missing committed START entry.
- Remaining R3/B4/F3: permanent tests still lack complete exact JSON/null values for all 12 rows, all earlier/later pairs, all root/HEAD/UNBORN stderr stages and all-pair byte snapshots. Later whole-status command failure paired with root/object-format/branch mismatch returned the semantic row rather than table-first `GIT_COMMAND`.
- Positive regression: independent digest/content-identical/later-drift/autocrlf/all-eight hidden flags and all 12 synthetic code/path/reason rows passed; B1/B2/F1/F2 remain closed.
- Evidence: focused **21 passed, 1 skipped** in 73.820s; root full pytest **220 passed, 2 skipped, 2 warnings, 33 subtests** in 195.05s. Green suites do not override the counterexamples.
- First next action: supervisor dispatches only the four-item residual correction in the controlling review, or returns the global command-precedence conflict to Astra as `DESIGN CHANGE REQUIRED`. Then rerun Sol. No commit/push.
- Boundaries remain structural only: `RISK BUDGET NOT APPROVED`; applicable evidence `NOT VERIFIED`; `SHADOW VALIDATION NOT PASSED`; `NOT PRODUCTION READY`.

## 2026-09-21 Terra B3/B4/F3 bounded review-fix

- Controlling post-review SHA-256 `6EFE50E741928633061D2D963235C111FC8FE94185907675F151849E4550B9B3`; no design change is required.
- Implemented: stdout/stderr retention for Git reads; stderr/nonzero fail closed as only `GIT_COMMAND`; state-path commands resolve before lower-priority semantic faults; quiet HEAD reads preserve true UNBORN.
- Tests: actual invalid-token plus failed `cat-file`, stderr fixtures across committed-state reads, nonzero later-status versus malformed-tree pair, non-leak assertions and snapshot checks. B1/B2/F1/F2 tests are retained.
- Focused correction test and py_compile passed. Run full unit, pytest, structural startup and whitespace checks, record their actual outputs, then request the single Sol recheck. No commit/push or engineering PASS.

## 2026-09-21 independent Sol PHASE0-GIT-IDENTITY-V2 post-validation — REVIEW_REQUIRED

- Controlling review: `docs/reviews/phase0_git_identity_post_review.md`, SHA-256 `6EFE50E741928633061D2D963235C111FC8FE94185907675F151849E4550B9B3`.
- Verdict: **`REVIEW_REQUIRED`**. Do not issue `PASS_ENGINEERING_PHASE0_GIT_IDENTITY`, create the initial commit or push.
- Closed: B1 normalized committed-state digest, B2 autocrlf/committed-byte behavior, F1 all eight assume/skip/fsmonitor combinations using `-f`, and F2 ASCII path/collision predicate.
- Blocking B3/F3: a disposable paired fault with valid tree, failed `cat-file` command and invalid STATE token returned `GIT_TOKEN`; frozen revision 4 requires earlier `GIT_COMMAND`. `_run_git` also discards stderr although revision 2 freezes stderr fail-closed behavior.
- Blocking B4/F3: permanent tests cover only eight single fault rows, observed key names and two pairs; ADR-0019 requires complete exact JSON for all 12 rows, every earlier/later pair and read-only snapshots across every success/failure path.
- Independent positive evidence: separate raw-byte digest matched; later drift failed; content-identical and autocrlf passed; all eight tag combinations, staged/unstaged/index-only/mode faults and byte snapshots behaved as frozen.
- Automated evidence: focused Phase-0 **20 passed, 1 skipped**; full pytest **219 passed, 2 skipped, 2 warnings, 25 subtests passed**; compile/startup/whitespace passed. Green suites do not override the reproduced frozen-contract failure.
- First next action: Terra changes only `scripts/verify_startup.py`, `tests/test_verify_startup.py` and necessary Phase-0 operational records per the six-item bounded correction spec in the controlling review, then returns to Sol for one independent recheck.
- Do not change: selected Policy, frozen rev2/rev3/rev4/pre-review/ADR, application/DB/provider/model/risk/wealth/return-bridge/trials/Production semantics or positive B1/B2/F1/F2 behavior. No commit/push.
- Boundaries remain structural only: `RISK BUDGET NOT APPROVED`; applicable evidence `NOT VERIFIED`; `SHADOW VALIDATION NOT PASSED`; `NOT PRODUCTION READY`.

## 2026-09-21 Terra PHASE0-GIT-IDENTITY-V2 implementation checkpoint

- Authority: ADR-0019 / D0011 froze revision 2 `8225D5DAE489BEB4B9533CFE2D3B84308B26A214B889FE437E69E9F9DFC12E6C`, revision 3 `A6E5A229934EAE977A7EEFA73F8310D64FB9B22655F8D863787768473EBB625D`, revision 4 `BDC2119A14D67D379A0E70EDB6B7E849C8C66ADF2432B62242C2037A61E94DC0`, and controlling Sol pre-review `EF09C8EF0B7B0685678012D05115882B7A8675E444F85AD625A8B7FEFD0F7B5C`.
- Completed: `scripts/verify_startup.py` implements normalized committed-tree `STATE_SHA256`, raw NUL-safe tree parsing, exact `100644` stage/OID verification, `-v`/`-t`/`-f` flag rejection, frozen ASCII path/collision checks, closed first-fault findings and no later governance evaluation. It preserves legacy SHA, true UNBORN, root/branch/object-format, read-only and denial behavior.
- Tests: `tests/test_verify_startup.py` uses disposable SHA-1/main repositories for first/later/content-identical state, add/delete/rename/mode/START edits, CRLF clone, staged/index/untracked, all eight hidden-flag combinations with a pre-flag non-machine START_HERE mutation, grammar/collision and fault/report/read-only cases. No main-worktree Git mutation is performed by verification.
- Current evidence: focused hidden-flag matrix passed after the mutation probe. Full regression, compile and whitespace commands are recorded in TEST_STATUS below; repository remains `main / HEAD UNBORN`, all project files uncommitted, no commit/push/remote action.
- First next action: Sol independently falsifies the implementation using ADR-0019 and the frozen complete JSON/paired-fault/read-only matrix. Treat all current tests as Terra implementation evidence only.
- Boundaries: structural consistency only. `RISK BUDGET NOT APPROVED`; provider/PIT/model/OOS/calibration `NOT VERIFIED`; `SHADOW VALIDATION NOT PASSED`; `NOT PRODUCTION READY`.

## 2026-09-21 independent Sol PHASE0-GIT-IDENTITY-V2 revision-4 exact-triplet re-review — PASS FOR SCOPED DECISION FREEZE

- Controlling complete review: `docs/reviews/phase0_git_identity_pre_review.md`, SHA-256 `EF09C8EF0B7B0685678012D05115882B7A8675E444F85AD625A8B7FEFD0F7B5C`. Exact triplet: revision 2 `8225D5DAE489BEB4B9533CFE2D3B84308B26A214B889FE437E69E9F9DFC12E6C`; revision 3 `A6E5A229934EAE977A7EEFA73F8310D64FB9B22655F8D863787768473EBB625D`; revision 4 `BDC2119A14D67D379A0E70EDB6B7E849C8C66ADF2432B62242C2037A61E94DC0`; pre-addendum review `E014F3A45A14A75E90298FA93D53C13D2DB9235BF10C03C5842E45038182A0D9`.
- Verdict: **`PASS FOR SCOPED DECISION FREEZE`**. B1-B4/F1-F3 close across the exact triplet. This is design review only: no ADR amendment, implementation acceptance, initial commit or push is implied.
- F1 independently reproduced across all eight assume/skip/fsmonitor combinations: only `H/H/H` with no flags passed. `-f` exposes fsmonitor-valid and every command preserved byte snapshots.
- F2 independently applied to all 76 prospective tracked paths: zero violations/collisions. Reserved/trailing/ADS/traversal/absolute/backslash/non-ASCII counterexamples failed; valid ASCII examples passed.
- F3 fixes single-fault order, exact code/path/reason, exit/readiness, observed keys, established/null rules and digest-mismatch fields. Implementation must make the complete JSON/paired-fault matrix permanent.
- First next action: Astra adds an exact-hash scoped ADR amendment freezing the triplet plus review and naming allowed implementation files. Only then dispatch Terra. Sol post-validation remains mandatory before initial commit/push.
- Files changed by Sol: appended the controlling review and updated START_HERE, HANDOFF, TEST_STATUS and TODO only. No Policy, DECISIONS, design triplet, verifier/test, application/database/provider/model/trial/Production-gate file changed.
- Boundaries remain structural consistency only, `RISK BUDGET NOT APPROVED`, `SHADOW VALIDATION NOT PASSED`, `NOT PRODUCTION READY`.

## 2026-09-21 independent Sol PHASE0-GIT-IDENTITY-V2 revision-3 exact-pair re-review — DESIGN CHANGE REQUIRED

- Controlling complete review: `docs/reviews/phase0_git_identity_pre_review.md`, SHA-256 `E014F3A45A14A75E90298FA93D53C13D2DB9235BF10C03C5842E45038182A0D9`. Reviewed exact pair: revision 2 `8225D5DAE489BEB4B9533CFE2D3B84308B26A214B889FE437E69E9F9DFC12E6C`, revision 3 `A6E5A229934EAE977A7EEFA73F8310D64FB9B22655F8D863787768473EBB625D`; pre-addendum review `EAC0AD2B8F383DBF0DB2DAADE899E5313114E4CAF42E6ACB9C16C9DC6D62F1B7`; Policy unchanged.
- Verdict: **`DESIGN CHANGE REQUIRED`**. B1-B2 remain closed in revision 2. Assume-unchanged and skip-worktree closure is sound, but B3-B4 remain blocked by F1-F3. No freeze, Terra dispatch, initial commit or push is authorized.
- F1 reproduced: with `core.fsmonitor=true`/fsmonitor-valid, both specified `ls-files -v` and `-t` returned accepted `H`, while the omitted `ls-files -f` returned lowercase `h`. Replace/add `-f` and test literal bytes for all flag combinations.
- F2: NFC/casefold is directionally useful, but “round-trip as workspace-relative Windows paths” does not define reserved devices, trailing dot/space, ADS/colon, controls, limits or a pinned Unicode table. Freeze one host-independent predicate.
- F3: first-fault code order and observed keys are improvements, but the design still lacks a literal condition/code/reason/exit/report-value table. Exact per-stage JSON remains underdetermined.
- Positive evidence: `-v` rejected assume-unchanged, `-v`/`-t` rejected skip-worktree, binary stage/tag framing was exact, and the read commands preserved disposable worktree/`.git` byte snapshots. Revision-2 state-transition evidence remains valid.
- First next action: Astra issues revision 4 changing only F1-F3/B3-B4 while preserving the exact revision-2 digest/autocrlf contract. Sol re-reviews the new pair. Terra remains undispatched until `PASS FOR SCOPED DECISION FREEZE` and ADR amendment.
- Files changed by Sol: appended the controlling review and updated START_HERE, HANDOFF, TEST_STATUS and TODO only. No Policy, DECISIONS, design pair, verifier/test, application/database/provider/model/trial/Production-gate file changed.
- Boundaries remain structural consistency only, `RISK BUDGET NOT APPROVED`, `SHADOW VALIDATION NOT PASSED`, `NOT PRODUCTION READY`.

## 2026-09-21 independent Sol PHASE0-GIT-IDENTITY-V2 revision-2 re-review — DESIGN CHANGE REQUIRED

- Controlling complete review: `docs/reviews/phase0_git_identity_pre_review.md`, SHA-256 `EAC0AD2B8F383DBF0DB2DAADE899E5313114E4CAF42E6ACB9C16C9DC6D62F1B7`. Reviewed revision 2 SHA-256 `8225D5DAE489BEB4B9533CFE2D3B84308B26A214B889FE437E69E9F9DFC12E6C`; draft-1 review pre-addendum SHA-256 `A61D8804DCB95CD6721AED5EE65B886D1F23C2272FE2DA923509B4D300FA99CE`; Policy remains `ACF013CE46F450423D83DEEBA1C207B22C669670C2B9BDC83A8A665F5AD38E67`.
- Verdict: **`DESIGN CHANGE REQUIRED`**. B1 normalized content-state digest and B2 committed-byte/autocrlf behavior close and must be preserved. B3-B4 remain blocking; no freeze, implementation, initial commit or push is authorized.
- C1 reproduced: after setting assume-unchanged or skip-worktree, a worktree-only START_HERE edit can leave scoped porcelain status empty, retain the stage-0 HEAD OID and retain a matching committed state digest. Revision 2 therefore does not meet its own staged/unstaged divergence guarantee. Exact index-flag inspection and stable failure tests are required.
- C2: the draft specifies only the mismatch detail, not exact codes/ordering/field values for every new parser/Git/tree/blob/index/status failure, and it does not define byte-level case-collision equivalence. Multiple incompatible implementations could satisfy “fail.” Add a closed fault/output table and complete JSON oracles.
- Independent positive matrix: first commit passed; unrelated later content, add/delete/rename/mode and non-token START edits failed stale state; correct token updates passed; content-identical commit retained the digest; clean autocrlf clone passed; binary NUL/tree/blob handling and a clean read-only byte snapshot passed.
- First next action: Astra issues revision 3 changing only C1-C2/B3-B4 while preserving B1-B2. Sol independently re-reviews the new exact hash. Terra remains undispatched until `PASS FOR SCOPED DECISION FREEZE` and ADR amendment.
- Files changed by Sol: appended the controlling review and updated START_HERE, HANDOFF, TEST_STATUS and TODO only. No Policy, DECISIONS, design, verifier/test, application/database/provider/model/trial/Production-gate file changed.
- Boundaries remain structural consistency only, `RISK BUDGET NOT APPROVED`, `SHADOW VALIDATION NOT PASSED`, `NOT PRODUCTION READY`.

## 2026-09-21 independent Sol PHASE0-GIT-IDENTITY-V2 pre-review — DESIGN CHANGE REQUIRED

- Controlling review: `docs/reviews/phase0_git_identity_pre_review.md`, SHA-256 `A61D8804DCB95CD6721AED5EE65B886D1F23C2272FE2DA923509B4D300FA99CE`. Reviewed draft SHA-256 `DF3BEA3718DBD8259D077D53EEDFC9E6C29EC9A5340AB7DD940A63685F29D790`; Policy remains `ACF013CE46F450423D83DEEBA1C207B22C669670C2B9BDC83A8A665F5AD38E67`.
- Verdict: **`DESIGN CHANGE REQUIRED`**. The self-reference diagnosis is correct, but bare `SELF` weakens exact committed-HEAD drift detection and is not cross-platform byte-stable. No amendment freeze, implementation, initial commit or push is authorized.
- B1 reproduced: after an initial SELF commit, a second commit changed only another tracked file. HEAD changed, while root/main/commit/tree-entry/START-byte/path-clean conditions all still passed. A pinned nonrecursive identity projection or equivalently protected external ref is required.
- B2 reproduced on Windows: with `core.autocrlf=true`, the committed LF blob was 24 bytes and a clean checkout was 25-byte CRLF; scoped Git status remained empty. Add reviewed START_HERE line-ending protection to `.gitattributes` and allowed scope, or change the exact-byte contract explicitly.
- B3-B4: freeze raw binary `cat-file`/NUL-safe `ls-tree` and status commands, regular `100644 blob`/literal-path semantics, exact failure/status precedence and recorded-vs-observed fields. Add later-commit drift, Windows checkout, tree-mode, staged-index-only, newline/binary, object-format and every-path read-only tests.
- Baseline evidence: existing `python -m unittest tests.test_verify_startup -v` remains **13 passed, 1 skipped**; this is PHASE0-V1 evidence only and contains no SELF cases. Temporary probes were isolated and removed. Workspace remains main/UNBORN.
- First next action: Astra issues a corrected amendment addressing B1-B4 without weakening PHASE0-V1 denial/read-only/root/branch/UNBORN/legacy behavior. Sol re-reviews its exact hash. Terra remains undispatched until `PASS FOR SCOPED DECISION FREEZE` and a new ADR amendment.
- Files changed by Sol: new review plus START_HERE, HANDOFF, TEST_STATUS and TODO operational status only. No Policy, DECISIONS, amendment, verifier/test, application/database/provider/model/trial/Production-gate file changed.
- Boundaries remain structural consistency only, `RISK BUDGET NOT APPROVED`, `SHADOW VALIDATION NOT PASSED`, `NOT PRODUCTION READY`.

## 2026-09-21 Astra Phase-0 identity revision-2 correction

- Created `docs/PHASE0_GIT_IDENTITY_AMENDMENT_REV2.md`. `STATE_SHA256` hashes the complete committed tree while normalizing only its own 64 digits in committed START_HERE, removing the cryptographic cycle without allowing later content drift.
- Digest uses binary HEAD tree/blob/index records, not raw worktree bytes. Clean autocrlf checkout is accepted through Git status/index equality; staged/unstaged/index/mode/path/object corruption fails closed.
- Next action: hash and independently re-review B1-B4. No verifier code, tests, ADR, commit, remote or push before PASS/freeze.

## 2026-09-21 Git push prerequisite — Phase 0 self-reference defect

- User supplied empty GitHub destination `https://github.com/oh12kj-dot/OPTIVEST`; `git ls-remote` returned no refs. No remote or commit has yet been created.
- Root cause: the frozen startup contract requires START_HERE to contain the exact current HEAD SHA, but a commit cannot contain its own not-yet-known SHA. Pushing the current UNBORN record would make a fresh clone fail startup integrity.
- Astra drafted `docs/PHASE0_GIT_IDENTITY_AMENDMENT.md`: add recorded literal `SELF`, valid only when START_HERE is tracked at current main HEAD and current bytes exactly equal HEAD bytes. Actual observed SHA remains in verifier output; unrelated dirty files stay visible.
- First next action: Sol independently review this narrow amendment. On PASS, freeze exact hash, dispatch Terra for verifier/tests/docs, run independent post-validation, then create and push the initial commit. Do not push a knowingly inconsistent governance snapshot.

## 2026-09-21 independent Sol revision-2 re-review — DESIGN CHANGE REQUIRED

- Controlling review: appended revision-2 section in `docs/reviews/research_return_bridge_pre_review.md`, complete SHA-256 `E037909BE7942E11048CE0A37ED5D04DC9B830E9EEA747297FF1DCC3B502B7CB`. Reviewed revision 2 SHA-256 `1D505E1FC643A6EE8509EC2381BDB73B23A4D56ADF2BBF907399151AD7BD491C`; pre-addendum review identity was `418E4D87745F8C974881F918CCA00C5426D6A9EE9EFDD0C7C0B59C388B058B5F`; Policy remains `ACF013CE46F450423D83DEEBA1C207B22C669670C2B9BDC83A8A665F5AD38E67`.
- Verdict: **`DESIGN CHANGE REQUIRED`**. B1-B6 are closed at their corrected narrow semantic level. B7-B8 remain blocking, so revision 2 is not ready for exact-hash freeze and Terra remains undispatched.
- Residual C1: Evaluation/result/capabilities/status/provenance remain non-unique. Exact nested schemas, output ordering, code-to-status/HTTP/CLI/hash matrix, manifest/tree hash construction and Git null rules are absent.
- Residual C2/C3: BASE/MAX omit required literal text/IDs/container/output objects, tie/Unicode/fault cases lack exact bytes, and the 10-second/256-MiB performance gate lacks a reproducible measurement protocol. Therefore no unique `C(BASE)` length/hash or complete R1-R16 oracle exists.
- Independent reproduction passed the intended core: BASE start FCFE/share 10 and price 100; prior .08; GROWTH log `ln(2)/7`, annualized `2^(1/7)-1`, disagreement `ln(2)/7-.08`; zero and positive-distribution semantics; MAX 32+4+32+4+12=84 rows/32 identity evaluations; exact serialized limits and semantic-set permutation principle.
- First next action: Astra issues revision 3 changing only residual B7-B8 closure, preserving B1-B6 and all denial states. Then Sol independently re-reviews the new exact hash. No DECISIONS/ADR change, implementation dispatch, or model/Production promotion before `PASS FOR SCOPED DECISION FREEZE`.
- Files changed by Sol: appended controlling review plus START_HERE, HANDOFF, TEST_STATUS and TODO operational status only. No revision design, Policy, DECISIONS, accepted/frozen artifact, application/test/DB/provider/trial/Production-gate file changed.
- Boundaries remain `RISK BUDGET NOT APPROVED`, applicable items `NOT VERIFIED`, `SHADOW VALIDATION NOT PASSED`, `NOT PRODUCTION READY`. This is a specification failure, not `MODEL VALIDATION FAILED`.

## 2026-09-21 independent Sol draft-1 pre-review — DESIGN CHANGE REQUIRED

- Controlling review: `docs/reviews/research_return_bridge_pre_review.md`, SHA-256 `418E4D87745F8C974881F918CCA00C5426D6A9EE9EFDD0C7C0B59C388B058B5F`. Reviewed draft SHA-256 remains `6946F1608D638244A3677A0338B37DAC84C7290B10ED134DA6293AE10229B26B`; selected Policy remains `ACF013CE46F450423D83DEEBA1C207B22C669670C2B9BDC83A8A665F5AD38E67`.
- Verdict: **`DESIGN CHANGE REQUIRED`**. Draft 1 is not ready for exact-hash decision freeze and authorizes no implementation. Decision version remains D0010; DECISIONS/ADR freeze state was not changed.
- Open blockers B1-B8: FCFE/accounting basis and scope labeling; distribution clock plus buyback/capital-allocation nonduplication; disjoint prior/shrinkage semantics; semantic ledger overlap/dependency closure; contradictory wire-order versus canonical-permutation rules; zero/nonfinite/CAGR/tie/empty-range outputs; incomplete Evaluation/Artifact/Capabilities/error/provenance/transport contracts and absent source manifest; non-decidable R1-R16 BASE/MAX/resource oracles and missing fixed serialized/algorithmic limits.
- R1-R14 and R16 require design change. R15 no-state/no-promotion is ready to retain after correction but does not cure the blockers. Sol independently reproduced the distribution-clock counterexample (terminal value 110 versus 113.400956406250 under a year-1 distribution held at 5%) and the prior-additivity ambiguity (.16 versus .11).
- First next action: Astra reads the controlling review, corrects every B1-B8 item without broadening into a calibrated forecast/path generator/optimizer, produces a new raw design hash, and requests independent Sol re-review. Terra remains undispatched until a later `PASS FOR SCOPED DECISION FREEZE` and exact ADR freeze.
- Files changed by Sol: the new review plus START_HERE, HANDOFF, TEST_STATUS and TODO operational status only. No Policy, accepted design/review, DECISIONS, application/test/model/service/phase2/risk-editor/wealth-lab/migration/dependency/lock, provider permission/raw capture, database, research-trial or Production-gate file changed.
- Boundaries: `RISK BUDGET NOT APPROVED`; forecast/model/provider/PIT/calibration/risk/OOS `NOT VERIFIED`; `SHADOW VALIDATION NOT PASSED`; `NOT PRODUCTION READY`. The failed design review is not `MODEL VALIDATION FAILED` and promotes no Policy section 18 item.

## 2026-09-21 Astra revision-2 correction — Sol re-review pending

- Preserved reviewed draft 1 and created `docs/RESEARCH_RETURN_BRIDGE_DESIGN_REV2.md`; no accepted/frozen artifact or implementation changed.
- B1-B8 correction: narrowed the claim to a terminal FCFE-per-share identity; fixed annual-run-rate/share/FCFE/distribution/buyback literals; made prior components incremental and disjoint; replaced overlap claims with an exact 12-row mechanical coverage registry while retaining economic overlap NOT VERIFIED; made all arrays semantic sets; closed tagged zero/nonfinite/tie/range behavior; froze Evaluation/Artifact/capabilities, failure stages, transport/CLI, source manifest, byte limits, and BASE/MAX/R1-R16 oracles.
- Revision-2 SHA-256 is `1D505E1FC643A6EE8509EC2381BDB73B23A4D56ADF2BBF907399151AD7BD491C`; whitespace, `git diff --check` and structural startup verification passed. First next action: send these exact bytes to Sol for one independent B1-B8/R1-R16 re-review. No ADR or Terra dispatch unless verdict is `PASS FOR SCOPED DECISION FREEZE`.

## 2026-09-20 Astra next-slice draft — Sol falsification required

- Current task: `docs/RESEARCH_RETURN_BRIDGE_DESIGN.md`, draft 1, is a design-only proposal for a stateless supplied-synthetic FCFE-per-share Economic Return Bridge, hierarchical statistical/economic prior diagnostic, signed layer disagreement and a closed nine-row uncertainty ledger. It intentionally emits no fitted forecast, probability distribution, monthly path, `U_objective`, `U_risk`, `S_stress`, decision margin, optimizer, ranking, action or DB write.
- Why this slice: ADR-0018 accounting is fully accepted but its inputs are supplied paths. The smallest next testable layer is return-bridge arithmetic and uncertainty-role closure; empirical calibration and path generation remain blocked by admissible data/provider evidence and require later designs.
- Status: `DRAFT / SOL INDEPENDENT REVIEW REQUIRED / NOT FROZEN`. No application/test implementation is authorized. Decision version stays D0010 and no ADR was added.
- First next action: dispatch **Sol high** for a blind/falsification-first review of the raw Policy requirements and draft formulas/contracts before reading this rationale. Sol must challenge FCFE basis/start consistency, distributions versus buybacks, prior additivity/shrinkage, scenario jointness, forbidden uncertainty roles, zero/nonfinite/numerical semantics, closed failures, provenance, no-state/no-promotion and R1–R16 decidability. Write `docs/reviews/research_return_bridge_pre_review.md` with exact reviewed SHA-256 and a scoped verdict.
- Do NOT Change during review: selected Policy, accepted designs/reviews/ADRs, source/tests/models/service/phase2/risk_editor/wealth_lab/migrations/dependencies, provider permissions/raw captures, user DBs, research ledger or Production register. Sol may add only its review and operational evidence records.
- Existing accepted state: ADR-0018 retains `PASS_ENGINEERING_RESEARCH_WEALTH_LAB`, controlling report SHA-256 `049195C11ED1690BEE629483EDFDC1BDD58FF7947868BF25F0154D52214EC279`. README/DECISIONS/FILE_MAP stale wording was reconciled without changing semantics.
- Boundaries: `RISK BUDGET NOT APPROVED`; provider/PIT, real forecast, calibration, numerical feasibility, investment model and OOS `NOT VERIFIED`; `SHADOW VALIDATION NOT PASSED`; `NOT PRODUCTION READY`.

### 2026-09-21 continuation / Git delivery state

- Draft identity: `docs/RESEARCH_RETURN_BRIDGE_DESIGN.md` SHA-256 `6946F1608D638244A3677A0338B37DAC84C7290B10ED134DA6293AE10229B26B`. Whitespace, `git diff --check` and structural startup verification passed after the draft checkpoint.
- Review dispatch: the requested Sol-high independent pre-review was correctly separated from Astra work but ended at the reviewer usage limit before producing `docs/reviews/research_return_bridge_pre_review.md`. Treat the review as **not performed**, not failed or passed. Retry the same bounded assignment when capacity is available; do not dispatch Terra first.
- Git delivery: the user requested push. The repository has no remote and `gh` is not installed, so a destination cannot be inferred safely. Prepare a local initial commit, then obtain the exact remote URL before push. Do not create or guess a public repository.
- Exclusions: `phase2_check.db` and the two quarantined Nasdaq raw `.txt` files are ignored. The raw files have unverified redistribution/licensing status and must not be pushed; the textual capture manifest remains documentable. `optivest.db` was already ignored.

## 2026-09-20 final independent actual-browser W18 recheck — all-surface PASS

- Controlling disposition: **`PASS_ENGINEERING_RESEARCH_WEALTH_LAB`**. Report: `docs/reviews/research_wealth_lab_post_review.md`, SHA-256 `049195C11ED1690BEE629483EDFDC1BDD58FF7947868BF25F0154D52214EC279`. Genuine Codex in-app Browser desktop/390px testing closes W18; the 2026-09-20 non-browser evidence below remains linked and unchanged.
- Browser matrix: desktop and explicit 390px BASE/W6c passed structured alternative/benchmark/unavailable, stress, ruin/monetary-zero/open-recovery and no-promotion rendering. W2 returned exact `INPUT_INCOMPLETE`/`MISSING` while preserving editor text. Editing immediately made results stale and disabled Download. Evaluate/Load/editor disabled in flight and re-enabled afterward. Literal XSS produced no injected DOM/dialog. HTTP rejection and a controlled disposable-proxy network outage were visible and stale-safe.
- Rehash/download: the normal response enabled Download only after browser-local rehash. The corrupt proxy retained server `COMPUTED_SYNTHETIC` but produced browser-local `ARTIFACT_HASH_MISMATCH`, hidden structured output and disabled Download. Independent desktop and 390px downloads `(1)` and `(2)` were each 201,850 bytes, SHA-256 `5A55229392B6DABA138A9365126023C7CC28D0A27DCCE1D5B9A2A4C77B68EB29`; a fresh BASE POST matched the desktop download byte-for-byte. At 390px the rendered document had `clientWidth=scrollWidth=375` after the vertical scrollbar (`390=390` before it); textarea scrolling remained internal.
- Isolation: only the disposable corrupt proxy was stopped for the network-failure case and it was restarted from the unchanged helper; normal port 8000 and its disposable SQLite database were not stopped or mutated. Browser viewport was reset and agent-created tabs were closed. No implementation/test/frozen authority/ADR/model/service/risk-editor/Phase-2/migration/dependency/lock/provider/database/trial/Production-gate file changed.
- Boundaries / next action: ADR-0018 has no remaining known engineering acceptance item. This does not validate or approve forecasts, optimizer, risk feasibility, provider/PIT, calibration, OOS, shadow or Production. `RISK BUDGET NOT APPROVED`, relevant items `NOT VERIFIED`, `SHADOW VALIDATION NOT PASSED`, `NOT PRODUCTION READY`. Any next material investment slice returns to Astra design/review rather than extending this PASS.

## 2026-09-20 final independent Sol B6/W17 recheck — non-browser PASS

- Controlling disposition: **`PASS_ENGINEERING_RESEARCH_WEALTH_LAB_NON_BROWSER`**. Report: `docs/reviews/research_wealth_lab_post_review.md`, SHA-256 `665C19AD9AB03411009317B5C71467EEC955BB24A864D9DC432810790564E987`. Residual B6/W17 is independently closed; there is no remaining known non-browser implementation blocker in the applicable frozen matrix.
- Independent race reproduction: for each of library/HTTP example/capabilities, a new disposable authority/source copy was loaded, the first real provenance check succeeded, and copied `wealth_lab.css` was mutated immediately afterward. The second real check ran in the same invocation (`calls=2`) and returned `SOURCE_CHANGED`; HTTP returned canonical 503 with null provenance/input hash/result. This was separate from the permanent pytest assertion and did not mutate repository bytes.
- Automated evidence: focused non-MAX **76 passed, 1 deselected, 2 warnings in 20.94s**; MAX **1 passed, 76 deselected, 2 warnings in 51.66s**, evaluator `47.823778s`, peak `106,716,617` bytes, Evaluation/Artifact `14,040,496`/`14,553,939`; full **212 passed, 2 skipped, 2 warnings in 127.78s**. Python compile, JavaScript syntax, startup verifier, exact frozen hashes, tracked diff and explicit relevant-file whitespace checks passed.
- Isolation: on disposable SQLite, the lab POST returned HTTP 200/`COMPUTED_SYNTHETIC` and preserved the exact 32-table/43-row map. Provider/readiness reads also preserved it and stayed 403 `BLOCKED_POLICY` / `NOT PRODUCTION READY` / `PROVISIONAL / RISK BUDGET NOT APPROVED`. The separately exercised preflight remained ineligible and added only its designed `decision_snapshots` row. Downgrade-base/upgrade-head/reseed ended at `0003_research_risk_editor`, 32 tables/43 rows. Trial ledger remained 0 bytes, SHA-256 `E3B0C442...B855`.
- Remaining acceptance evidence: full `PASS_ENGINEERING_RESEARCH_WEALTH_LAB` is **not issued**. Mandatory actual-browser W18 remains `NOT VERIFIED`: desktop and 390px BASE/W2/W6c/stale/in-flight/error/XSS/ruin/rehash/download-rejection/structured rendering. Run this only with a genuine browser backend; static tests are not a substitute.
- Boundaries / next action: no forecast/model/risk/provider/PIT/calibration/OOS/shadow/Production promotion. `RISK BUDGET NOT APPROVED`, relevant items `NOT VERIFIED`, `SHADOW VALIDATION NOT PASSED`, `NOT PRODUCTION READY`. Do not redo B6/W17. When a genuine browser backend becomes available, execute only the frozen W18 matrix and record the final all-surface disposition.
- Files changed by Sol: controlling post-review plus `.ai/START_HERE.md`, `.ai/HANDOFF.md`, `.ai/TEST_STATUS.md`, `.ai/TODO.md`. Implementation/tests/frozen authority/ADR semantics/model/service/risk-editor/Phase-2/migrations/dependencies/lock/provider/user DB/trials/Production gate were not changed. Design deviations: **NONE found** in the rechecked scope.

## 2026-09-17 handoff refresh — resume with Sol recheck, do not redo Terra correction

- Current factual state: the final Terra B6/W17 correction is present in the working tree despite the implementation subagent ending with a usage-limit error after its work and automated evidence had already completed. `optivest/wealth_lab.py` and `tests/test_wealth_lab.py` contain the second pre-emission source checks and four copied-source library/HTTP regressions. Operational evidence below records focused **76 passed, 1 deselected**, MAX **1 passed, 76 deselected**, and full **212 passed, 2 skipped**. Do not discard or reimplement this correction merely because the agent's final transport status was errored.
- Controlling authority: Policy `ACF013CE46F450423D83DEEBA1C207B22C669670C2B9BDC83A8A665F5AD38E67`; frozen design `4097F897073E681ABAB4B5535EAA81ED38F32F2A9BAE72B49C672B6FFF74B80C`; pre-review `AACE3319B6A675F6EE6873E5FAEDBA3241131483BCF8827E6150FCBD8C331E83`; current post-review/recheck report `B6E2DBE01128DF5C0CAC3649EF70A07940ED2829C73C739DF0B03EFE8187016D`.
- First next action: dispatch **Sol high** for one independent recheck of only the residual B6/W17 copied-source cases first, then the applicable focused/MAX/full/static/no-write regression matrix. Sol must independently mutate a disposable copied manifest immediately after the first real provenance check and confirm both library and HTTP example/capabilities fail `SOURCE_CHANGED`/503 on the same invocation. If this closes, record the scoped non-browser disposition; do not infer W18 PASS.
- Remaining external evidence gap: actual desktop and 390px W18 BASE/W2/W6c/stale/in-flight/error/XSS/ruin/rehash/download-rejection/structured-rendering interaction is still `NOT VERIFIED` because browser discovery returned zero backends. Static tests do not replace it. Retry only when a genuine browser backend is available.
- Acceptance boundary: the controlling verdict remains `IMPLEMENTATION VALIDATION FAILED / REVIEW_REQUIRED` until Sol writes the recheck disposition. No `PASS_ENGINEERING_RESEARCH_WEALTH_LAB`, model/risk/provider/PIT/OOS/shadow or Production promotion is authorized. Literal states remain `RISK BUDGET NOT APPROVED`, relevant items `NOT VERIFIED`, `SHADOW VALIDATION NOT PASSED`, `NOT PRODUCTION READY`.
- Responsibilities: Astra design/freeze complete; Terra final correction complete in the worktree; Sol independent residual recheck next; browser W18 evidence pending environment availability. No active subagent should modify implementation during the Sol recheck.

## 2026-09-17 Terra final minimal B6/W17 correction — Sol recheck pending

- Changed: `example_bytes()` and `capabilities()` now perform a second real `_provenance()` Policy/design/manifest check immediately before returning success. Four permanent disposable-copy regressions cover library and HTTP GET for both surfaces: after the first clean real provenance check, a copied CSS manifest mutation forces root `SOURCE_CHANGED`; HTTP returns canonical Evaluation 503 with null provenance/input hash/result. No other semantics or UI changed.
- Evidence: focused non-MAX **76 passed, 1 deselected in 18.88s**; MAX **1 passed, 76 deselected in 38.04s**, evaluator `34.792737s`, peak `106,716,558` bytes, Evaluation/Artifact `14,040,496`/`14,553,939`; full **212 passed, 2 skipped, 2 known dependency warnings in 108.05s**. Compile/JS/startup/hash/diff evidence is in TEST_STATUS.
- Validation / remaining: no `PASS_ENGINEERING` claim. Independent Sol recheck remains required. Actual desktop/390px W18 remains `NOT VERIFIED` because no browser backend was available. All investment/model/provider/PIT/OOS/shadow/Production boundaries remain unchanged.

## 2026-09-17 independent Sol correction recheck — FAILED / REVIEW_REQUIRED

- Verdict / report: controlling `docs/reviews/research_wealth_lab_post_review.md`, SHA-256 `B6E2DBE01128DF5C0CAC3649EF70A07940ED2829C73C739DF0B03EFE8187016D`. No `PASS_ENGINEERING_RESEARCH_WEALTH_LAB`. B1-B5, B7 and static/non-browser B8 independently close. B6/W17 remains blocking: `example_bytes()` and `capabilities()` check source only once, so a copied manifest mutation immediately after the real check still yielded successful HTTP 200 example/capabilities instead of `SOURCE_CHANGED`/503. W18 actual browser also remains `NOT VERIFIED`; it is not the only blocker.
- Exact reproduction: copy all ten manifest members plus Policy/design to a temporary root; set the loaded hash context from the clean copy; wrap the real `_provenance()` so its first successful return appends one byte to copied `optivest/static/wealth_lab.css`; GET example returned 200/17,975 bytes and GET capabilities returned 200/4,645 bytes, with one provenance call each. The equivalent evaluate mutation returned root `SOURCE_CHANGED`, proving the residual is limited to the GET success paths. Original repository bytes were untouched.
- Independent evidence: W9d PRE/POST/drawdown/recovery, normalized semantic identity, the exact benchmark precision counterexample, escaped surrogate/combined depth-node precedence, simultaneous reversals, load/evaluation pre-emission source changes, Git states, HTTP/CLI contracts, static structured UI, RFC-6901 pointer fidelity, Stage-5/6/7 aggregation and W3/W4/W7/W8/W10/W11/W14/MAX oracles otherwise passed. Fast focused **72 passed, 1 deselected in 18.70s**; MAX **1 passed, 72 deselected in 40.37s** (evaluator 36.962977s, peak 106,717,753 bytes, Evaluation/Artifact 14,040,496/14,553,939); full **208 passed, 2 skipped in 110.85s**; two known dependency warnings in each applicable run. Compile, JS syntax, startup and tracked diff check passed.
- Isolation evidence: disposable SQLite lab POST preserved exact counts over 32 application tables/43 rows; provider display stayed 403/BLOCKED_POLICY; preflight stayed false; readiness stayed NOT PRODUCTION READY; downgrade/upgrade/seed ended at `0003_research_risk_editor` and 32 application tables. Trial ledger stayed empty. Policy/design/pre-review bytes remain `ACF013CE...38E67` / `4097F897...74B80C` / `AACE3319...31E83`; the prior post-review baseline was unchanged at `EEF7BF9D...520B` before this addendum.
- Exact next action: Terra makes only the frozen-scope example/capabilities second pre-emission source recheck and permanent copied-source library/GET tests, reruns all evidence, and requests one Sol recheck. Separately, obtain the mandatory desktop/390px W18 matrix only when a genuine browser backend exists. Do not change Policy/design/pre-review/ADR/models/service/risk_editor/phase2/migrations/dependencies/lock/provider/user DB/trials/production gate.
- Boundaries: `RISK BUDGET NOT APPROVED`; investment model/risk feasibility/provider/PIT/tradability/calibration/OOS `NOT VERIFIED`; `SHADOW VALIDATION NOT PASSED`; `NOT PRODUCTION READY`.

## 2026-09-16 Terra bounded B1-B8 correction — Sol recheck pending

- Changed / Why: permanent review-counterexample tests now enforce actual common PRE_TRADE holdings/cash and W9d initial-cost drawdown; one normalized Bundle for semantic-set/D/text hash, evaluation and Artifact input; unrounded path/member/benchmark log-exp aggregation plus exact private ledger assertions; escaped-surrogate rejection and bounded lexical depth-before-node precedence; both simultaneous terminal-zero findings; module-load and pre-emission authority/source checks with Git UNBORN/GIT_NOT_AVAILABLE separation; authority-first incrementally bounded HTTP/CLI acquisition and closed file codes; and structured safe-DOM UI panels with request/input revision protection. Frozen Policy/design/reviews/ADR, models/service/risk editor/Phase2/migrations/dependencies/provider/trials/Production gate were not changed.
- Permanent evidence: eight `test_post_review_b*` regressions reproduce every Sol counterexample, including the exact benchmark precision oracle and static UI race/presentation contract. Fast focused is 72 passed/1 deselected in 18.69s; MAX is 1 passed/72 deselected with 160 paths, 13,760 alternative events, 1,700 benchmark events, 53,760 cells, evaluator 34.808847s, peak 106,715,219 bytes, Evaluation 14,040,496 bytes and Artifact 14,553,939 bytes. Final full was 208 passed/2 skipped in 108.25s; compile/JS/startup/hash/diff results are in TEST_STATUS.
- Operational evidence: a disposable migrated/seeded SQLite POST returned HTTP 200/COMPUTED_SYNTHETIC with exact row-count maps unchanged across 32 application tables. A disposable downgrade-base/upgrade/seed ended at `0003_research_risk_editor`, 32 application tables. The temporary DB was removed. Authority hashes remain Policy `ACF013CE...38E67`, design `4097F897...74B80C`, pre-review `AACE3319...31E83`, post-review `EEF7BF9D...520B`.
- Validation / remaining: no `PASS_ENGINEERING` self-claim. One independent Sol recheck is mandatory. Actual browser desktop/390px stale/in-flight/error/XSS/ruin/rehash/download-rejection/structured rendering remains `NOT VERIFIED` because no browser backend was available; static tests are not substituted. `RISK BUDGET NOT APPROVED`, model/provider/PIT/OOS `NOT VERIFIED`, `SHADOW VALIDATION NOT PASSED`, `NOT PRODUCTION READY` remain literal. Design deviations: NONE intended; Sol decides conformance.

## 2026-09-16 ADR-0018 Sol post-validation — IMPLEMENTATION VALIDATION FAILED / REVIEW_REQUIRED

- Verdict / report: independent falsification is complete in `docs/reviews/research_wealth_lab_post_review.md`, SHA-256 `EEF7BF9D550AC4F615904FEABBBDD5EA33F37DB041C235B81AD3BF1346FF520B`. No `PASS_ENGINEERING_RESEARCH_WEALTH_LAB` exists. B1-B8 reproduce: wrong PRE_TRADE holdings and omitted initial-cost drawdown/recovery; missing semantic/D/text canonical normalization and an `input_sha256 != SHA256(C(Artifact.input))` case; benchmark rounding before expectation plus incomplete dual-precision/private invariant seams; escaped-surrogate and combined depth/node decoder failures; incomplete simultaneous terminal-zero aggregation; missing pre-emission source recheck plus wrong Git/example authority states; HTTP/CLI precedence/error deviations; and an incomplete/racy raw-JSON UI. These are implementation deviations under the existing freeze, not authorization to change semantics.
- Independent evidence: Policy/design/pre-review hashes match `ACF013CE...38E67` / `4097F897...74B80C` / `AACE3319...31E83`. W3/W4/W8/W10/W11 and MAX hand calculations agree. Focused fast **64 passed, 1 deselected**; MAX **1 passed, 64 deselected**, evaluator 34.314532s, peak 110,912,802 bytes, Evaluation/Artifact 14,036,616/14,550,059 bytes; full **200 passed, 2 skipped, 2 warnings**. Compile, JS syntax, startup and tracked diff check passed. CLI/library/HTTP BASE bytes matched. Disposable lab POST was no-write; provider display remained 403/BLOCKED_POLICY; preflight remained false; migration downgrade/upgrade/seed reached `0003_research_risk_editor` and 33 application tables; trial bytes unchanged.
- Browser / evidence limit: current root browser tooling exposed zero browser backends after required troubleshooting. Earlier Terra desktop/390px BASE load/evaluate/rehash/download success stays narrow evidence. Stale/in-flight/error/XSS/ruin/download-rejection and structured rendering are `NOT VERIFIED`; static evidence is not substituted. Static inspection independently finds a stale request/edit race and missing frozen structured panels.
- Current work / exact next action: Terra high performs one bounded correction pass in the ADR-0018 allowed files only, following report section `Required bounded correction and recheck` in order. Add permanent counterexamples for every B1-B8 item, rerun focused/MAX/full/static/disposable DB/source-copy evidence, update operational records, then request one Sol recheck. Do not edit Policy/design/pre-review/ADR/models/service/risk_editor/phase2/migrations/dependencies/lock/provider/user DB/trials/production gate. Any semantic alternative requires `DESIGN CHANGE REQUIRED` rather than an implementation choice.
- Mandatory boundaries: `RISK BUDGET NOT APPROVED`; investment model/risk feasibility/provider/PIT/tradability/calibration/OOS `NOT VERIFIED`; `SHADOW VALIDATION NOT PASSED`; `NOT PRODUCTION READY`. Design freeze remains intact; implementation design deviations are present.

## 2026-09-16 ADR-0018 Stage-5 exhaustive aggregation complete — browser W18 and Sol remain

- Changed / Why: replaced the split envelope/first-exception Stage-5 path with one deterministic nested collector in `optivest/wealth_lab.py`. It visits known object keys in scalar order and arrays in wire order, aggregates all independently decidable `UNKNOWN_FIELD`, `REQUIRED_FIELD`, `TYPE_INVALID`, `VALUE_RANGE`, `SET_MEMBERSHIP` and `FIELD_NOT_APPLICABLE` findings, deduplicates/sorts by JSON pointer then code, and never descends an invalid container or unknown key. Valid missing V tags now pass directly to Stage 6 without zero placeholders; typed-invalid findings still win. Decoder single-failure precedence, Stage-7 aggregation and frozen accounting semantics are unchanged.
- Permanent evidence: `tests/test_wealth_lab.py` now includes a simultaneous nested adversarial fixture spanning mandate, assets, positions, ordinary/stress path months and asset points, probability distributions/masses, alternatives/costs, benchmark levels and ledger rows; a separate invalid-container/unknown-key no-descent fixture; and a cross-surface Stage-6 missing-tag fixture.
- Tests / Results: fast focused `64 passed, 1 deselected in 7.68s`; MAX `1 passed, 64 deselected in 38.25s`; full `200 passed, 2 skipped` with two known upstream TestClient warnings in 96.93s. Python compile, JavaScript syntax, startup integrity and `git diff --check` passed. Startup reported `main`/`UNBORN`/18 untracked paths and `NOT PRODUCTION READY`. Authority bytes remain Policy `ACF013CE...38E67`, design `4097F897...74B80C`, review `AACE3319...31E83`.
- Validation status / remaining: the recorded non-browser implementation blocker is closed and the implementation is ready for independent Sol post-validation. The only remaining pre-validation evidence gap is W18 actual-browser stale/in-flight/error/XSS/ruin/download-rejection behavior; it remains `NOT VERIFIED`. Earlier desktop/390px BASE rehash/download success is not extended by inference. No `PASS_ENGINEERING`, §18, model or Production claim is made; `RISK BUDGET NOT APPROVED`, model/provider/PIT/OOS `NOT VERIFIED`, `SHADOW VALIDATION NOT PASSED`, `NOT PRODUCTION READY` remain literal. Design deviations: NONE.

## 2026-09-15 ADR-0018 Terra final bounded implementation evidence — Sol post-validation pending

- Changed / Why: completed deterministic Stage-7 aggregation for independent valid-shape cross-record defects (initial wealth, every supplied probability distribution, ordinary/stress asset and benchmark terminal-zero reversals, no-trade identity, cost-without-trade and funding). Added permanent independent W6e/f absorption/price-bound, W10 geometric-not-arithmetic, W12 stress separation and W13 closed-output tests. The older Stage-5 envelope aggregation remains limited; arbitrary nested typed/domain faults still stop at the first `LabError` rather than aggregating every independent finding. No frozen investment semantics changed.
- Evidence / Tests / Results: focused wealth-lab suite split to avoid masking MAX resource time: **60 passed, 1 deselected in 8.30s** plus MAX **1 passed, 60 deselected in 36.90s**. Final full `uv run pytest -q` -> **196 passed, 2 skipped, 2 known TestClient deprecation warnings in 100.35s**. Compile, JavaScript syntax, startup verifier and `git diff --check` passed. Authority hashes remain Policy `ACF013CE46F450423D83DEEBA1C207B22C669670C2B9BDC83A8A665F5AD38E67`, design `4097F897073E681ABAB4B5535EAA81ED38F32F2A9BAE72B49C672B6FFF74B80C`, review `AACE3319B6A675F6EE6873E5FAEDBA3241131483BCF8827E6150FCBD8C331E83`.
- W15/W16: a new disposable populated SQLite database completed `upgrade -> seed -> downgrade base -> upgrade -> seed`; it ended at `0003_research_risk_editor` with 32 application tables. HTTP TestClient evaluation was 200/`COMPUTED_SYNTHETIC` and exact before/after table-row maps were identical. No user database, provider, network, decision or production write was used.
- W17: disposable copied-checkout byte evidence: clean evaluation computed; an in-process manifest mutation returned `SOURCE_CHANGED`; a new loaded module after that mutation computed; mutated copied Policy/design returned `POLICY_MISMATCH`/`DESIGN_MISMATCH`; a missing copied manifest source returned `SOURCE_UNAVAILABLE`; no-copy-Git state reported `UNBORN`. Original authority files were never modified.
- Browser: prior actual desktop/390px BASE evaluate/rehash/download success remains recorded. The final retry returned `No browser is available`; stale/in-flight/error/XSS/ruin/download-rejection interaction cases are explicitly **NOT VERIFIED**. This is Terra implementation evidence only: no PASS_ENGINEERING claim and no §18/model/Production promotion. Sol must independently post-validate.
- Remaining non-browser limitation: exact section 6.1 Stage-5 exhaustive nested typed/domain aggregation is not yet implemented; consequently this is not honestly ready for Sol all-item acceptance even though the Stage-7/copy/migration/resource batches above are complete. Preserve this distinction in any continuation.

## 2026-09-15 ADR-0018 Terra bounded continuation — stronger evidence, not yet Sol-ready

- Changed / Why: retained unrounded 120-precision Decimal member/lower-envelope values until `difference_to_no_trade` subtraction, fixing the frozen W11 separate-minimum semantics rather than subtracting rounded public strings. Added independent W3/W4 cost-once cash/dividend/terminal-mark, W5 self-financing, W6 settlement/ruin/cash-loss, W7/W8 joint-path/tiny-positive, W9 recovery, W11 sign, W14 exact-fraction, W17 Unicode/authority/loaded-source and W19 MAX/fault regression coverage. No frozen investment or database semantics changed.
- Evidence / Tests / Results: focused `python -m pytest tests/test_wealth_lab.py -q` -> **57 passed in 50.52s**. Full `python -m pytest -q` -> **192 passed, 2 skipped, 2 known TestClient deprecation warnings in 116.47s**. MAX independently exercised 160 Alternative PathResults, 13,760 alternative events, 20 comparator paths/1,700 comparator events and the exact 53,760 cell bound; canonical Evaluation/Artifact bytes were 14,036,616 / 14,550,059. With tracemalloc, elapsed 40.777389s and peak was 110,916,726 bytes: >10s is recorded as the design review trigger; no coverage was reduced. Private invariant, Decimal nonconvergence, RESULT_SIZE and ARTIFACT_SIZE seams fail closed.
- W15/W16 evidence: on the disposable SQLite `0003_research_risk_editor` database (33 application tables), before/after exact row-count maps around TestClient wealth-lab evaluation were identical. Existing migration/seed was isolated from user data; no network/provider path was invoked. W18 success browser evidence remains the earlier desktop/390px BASE rehash/download proof. A later attempt to acquire a fresh browser session for stale/XSS rejection was unavailable after the initial browser disconnected; do not fabricate those remaining browser cases.
- Remaining exact blockers: nested typed and cross-record multi-finding aggregation is still not exhaustive; W6e/f, W10 and parts of W12/W13 are not the complete independent frozen 8.1 fixture family; W17 source mutations are currently loaded-context/authority seams, not disposable-checkout byte mutations; migration downgrade/upgrade is not rerun in this continuation; W18 stale/rejection/XSS/ruin error/download evidence needs a browser session. These prevent a truthful `Sol post-validation ready` claim. No PASS_ENGINEERING claim.

## 2026-09-15 ADR-0018 Terra implementation milestone — still incomplete, Sol review not ready

- Changed / Why: tightened the allowed stateless `wealth_lab` kernel's cross-record identity/cost/funding/terminal-zero checks, incomplete-input identity, Decimal and size/invariant test seams; completed static mounting in the narrow app integration; made CLI exits distinguish computation failures; added browser-side canonical artifact rehash/stale/in-flight behavior. No model, database writer, provider, decision/preflight, risk-editor, migration, trial or Production path was changed.
- Evidence / Tests / Results: `uv run pytest tests/test_wealth_lab.py -q` -> **46 passed**; a separately captured full `python -m pytest -q` run -> **181 passed, 2 skipped** in 68.51s (two known TestClient deprecation warnings). Python compile and `node --check` passed. An isolated temporary SQLite upgrade/seed proved HTTP library-byte parity, accepted OWS media and 415 gzip rejection; CLI evaluation succeeded while `OPTIVEST_DATABASE_URL` named an invalid drive, so lab dispatch did not open that database. Actual in-app browser desktop and 390px checks loaded/evaluated BASE, locally rehashed it, enabled download and kept the non-investment wording; 390px `scrollWidth=375` at width 390 (textarea internal scroll only). Structural startup verifier passed; Policy/design/review hashes match; main remains UNBORN/untracked.
- Current work / remaining: this remains materially short of the frozen all-item acceptance: comprehensive nested typed and cross-record multi-finding aggregation, all independent W3–W14 algebraic fixtures, source-mutation/restart and exact authority failure tests, MAX construction/resource/serialized-size measurement, isolated migration/no-write evidence, stale/XSS/ruin/error/download browser cases and a captured full suite are outstanding. Do not request or claim PASS_ENGINEERING; Sol post-validation remains pending.
- Validation status / limitations: unchanged literal gates: `RISK BUDGET NOT APPROVED`; model/risk feasibility/provider/PIT/tradability/calibration/OOS `NOT VERIFIED`; `SHADOW VALIDATION NOT PASSED`; `NOT PRODUCTION READY`. Design deviations: NONE.

## 2026-09-14 RESEARCH-WEALTH-LAB-V1 Terra implementation checkpoint — incomplete, Sol review not ready

- Changed / Why: Added only ADR-0018-authorized `wealth_lab` core/API/static/fixture/test files, narrow app route registration, DB-free CLI dispatch, README and operational records. The supplied-synthetic raw-byte evaluator establishes an initial exact-Fraction cost-once ledger/artifact path without touching database/provider/decision code.
- Evidence / Tests / Results: focused `tests/test_wealth_lab.py` 4 passed; Python compile, JavaScript syntax and structural startup verifier passed. A fresh disposable SQLite migration/seed TestClient probe reached page/capabilities and exposed then corrected an HTTP success-artifact status mapping defect. CLI example/evaluate completed with a canonical artifact. Full suite did not yield a captured completion result. Browser runtime was unavailable, so no actual desktop/390px evidence exists.
- Current work / remaining: exhaustive frozen W1-W19/8.1 behavior is not yet implemented or independently evidenced (notably complete parser stage semantics, closed typed ledger/benchmark/result contracts, terminal-zero validation, Decimal convergence seams, source-loaded mutation, MAX/resource/size and browser checks). Do not represent this checkpoint as all-item implementation or PASS_ENGINEERING. First next action is to complete every missing W oracle and rerun focused/full/HTTP/CLI/disposable-DB/browser evidence before requesting Sol falsification.
- Follow-up: corrected terminal-zero evaluation (ruin CAGR output had attempted to re-render a Number wrapper), added terminal-zero reversal enforcement and loaded-manifest change refusal, canonicalized example output, generalized HTTP media grammar, and increased focused suite to 29 passed. These additions reduce known gaps but do not close the exhaustive matrix.
- Bounded contract follow-up: the exact ledger registry now rejects unknown/wrong role-treatment-status rows; recursive raw decode, static source manifest, media/content-encoding probe and missingness precedence gained permanent tests (33 focused passes). Next exact batch is closed Result/Benchmark/Artifact shape plus all remaining parser pointer/aggregation/size/fault and MAX oracles. No Sol review yet.
- Comparator/result follow-up: added an unweighted comparator-only BenchmarkResult with path summaries/lower envelope, exact Result/AlternativeResult/BenchmarkResult key assertions, supplied multi-member order/difference regression and nested extra/missing-key negatives; focused suite is now 36 passed. The still-unclosed exact scope is nested duplicate pointer fidelity, aggregate independent schema findings, full benchmark stress output, output-size/fault seams, MAX/resource and full W1-W19 fixture matrix. Do not request Sol yet.
- Stress follow-up: completed stress path materialization for alternative and comparator output and tested it remains outside member/lower-envelope algebra (37 focused passes). Parser nested duplicate-pointer fidelity and required multi-finding aggregation remain the immediate unclosed work; no Sol review.
- Duplicate-pointer follow-up: raw decoder now detects the first nested duplicate after complete JSON syntax validation and returns deterministic RFC-6901 escaped pointer diagnostics; focused suite 41 passed. Representative alternatives, probability/member/mass, joint path/month/asset and metadata nesting are permanent W1 cases. Next exact item remains independent typed/cross-record multi-finding aggregation; no Sol review.
- Aggregation follow-up: Bundle-envelope independent unknown/missing/literal typed findings now aggregate/sort deterministically (42 focused passes), preserving single-failure decoder/transport precedence. Nested typed and cross-record aggregation remains incomplete; no Sol review.
- Validation status / limitations: Sol post-validation PENDING and currently not ready. RISK BUDGET NOT APPROVED; provider/PIT/tradability, model/risk feasibility, calibration/OOS NOT VERIFIED; SHADOW VALIDATION NOT PASSED; NOT PRODUCTION READY. No §18 item, research trial, production state or investment semantics was promoted. Design deviations: NONE.

## 2026-09-14 ADR-0018 freeze — Terra implementation next

- Changed / Why: Astra added ADR-0018 to `.ai/DECISIONS.md`, advancing the factual registry version D0009 -> D0010, and updated operational records for Terra. This freezes the passed synthetic accounting specification after Sol independently closed B1-B6; it does not implement or validate the lab.
- Exact authority: selected Policy `ACF013CE46F450423D83DEEBA1C207B22C669670C2B9BDC83A8A665F5AD38E67`; design revision2 `4097F897073E681ABAB4B5535EAA81ED38F32F2A9BAE72B49C672B6FFF74B80C`; controlling Sol pre-review `AACE3319B6A675F6EE6873E5FAEDBA3241131483BCF8827E6150FCBD8C331E83`. Passed design/review bytes retain their historical draft wording; ADR-0018 controls the freeze. All six review conditions are incorporated without waiver.
- Terra objective/scope: implement the closed raw-byte synthetic Bundle, exact rational self-financing wealth/cost-once/strict ruin/recovery kernel, supplied finite-member annual-log diagnostics, public Decimal/display/convergence rules, exact source/canonical artifact and failure contracts, DB-free CLI, session-free API and same-process laboratory UI. Fixed ADR-0012 synthetic 7Y/USD/pre-tax/no-flow/USD1m assumptions remain visibly separate from current edited budget. No optimizer/rank/action/risk feasibility or provider path.
- Exact files: create `optivest/wealth_lab.py`, `optivest/wealth_lab_api.py`, `optivest/static/wealth_lab.js`, `optivest/static/wealth_lab.css`, `optivest/fixtures/wealth_lab_example.json`, `tests/test_wealth_lab.py`; change `optivest/app.py` only for route registration, `optivest/cli.py` only for DB-free dispatch; README and .ai operational records/FILE_MAP. Manifest-only existing files are read-only. No model/service/risk_editor/phase2/migration/dependency/lock or retained-reviewer-test change is authorized.
- Acceptance: every frozen W1-W19 and section8.1 BASE/MAX oracle, independently derived numerical checks, failure/canonical/provenance/source/Git/size/fault cases, no DB/network writes or promotion, existing regression and isolated migration/no-write checks, actual HTTP/CLI and desktop/390px browser stale/error/XSS/ruin/download/rehash behavior. Record focused/full suites, compile/JS/startup/hash/diff checks, skips, MAX byte sizes and resources. Sol independent post-validation is mandatory; tests alone cannot establish engineering acceptance.
- First next action: Terra high reads AGENTS, selected Policy/hash, ADR-0018, full pinned design and controlling Sol review, then inspect only app/CLI integration and current isolated-test helpers. Implement the standard-library byte boundary/canonicalizer and exact kernel with BASE/oracle tests first, continue through all frozen surfaces and evidence, updating handoff at each material milestone. Return material ambiguities as DESIGN CHANGE REQUIRED rather than altering frozen semantics. No routine user approval is needed for this now-frozen reversible implementation scope.
- Required prohibitions: no passed design/review/Policy/old ADR changes, user DB access/destruction, live data/network/provider permission changes, real decision/preflight eligibility path, current-budget numerical impact, risk activation, calibration/evidence promotion, RESEARCH_TRIALS/PRODUCTION_GATE change, deployment/billing/orders/credentials/commit/history rewrite. Only disposable DBs for mandated regression. No recursive subagent or parent parallel implementation.
- Validation status / Model limitations: `RESEARCH-WEALTH-LAB-V1 FROZEN`; implementation and independent post-validation PENDING; no new PASS_ENGINEERING. No forecast/prior/economic/FX model, calibrated U sets, risk limits/feasibility, optimized alternatives, OOS/shadow or empirical trial. Every §18 item remains unpromoted. Literal gates: RISK BUDGET NOT APPROVED; model/risk feasibility/provider/PIT/tradability/calibration/OOS NOT VERIFIED; SHADOW VALIDATION NOT PASSED; NOT PRODUCTION READY. Design deviations: NONE.
- Responsibilities: Astra design/freeze complete; Sol pre-review complete; Terra authorized/not yet dispatched by freeze agent; Sol post-validation pending. Git main/UNBORN/untracked preserved. Tests in this freeze task are governance/hash/whitespace checks only, recorded in TEST_STATUS; no application implementation/test or DB/network operation occurred.
- Final freeze verification: startup checker exit0/structural PASS/findings[]/Production denied; git diff whitespace exit0 with untracked-file limitation; explicit selected operational-file whitespace scan found none; Policy/design/review re-hashes remained exact.

## 2026-09-14 RESEARCH-WEALTH-LAB-V1 Sol revision-2 re-review — PASS FOR SCOPED DECISION FREEZE

- Verdict: **`PASS FOR SCOPED DECISION FREEZE`** for corrected revision 2 `docs/RESEARCH_WEALTH_LAB_DESIGN.md` SHA-256 `4097F897073E681ABAB4B5535EAA81ED38F32F2A9BAE72B49C672B6FFF74B80C`. Controlling review is `docs/reviews/research_wealth_lab_pre_review.md` revision-2 section, SHA-256 `AACE3319B6A675F6EE6873E5FAEDBA3241131483BCF8827E6150FCBD8C331E83`.
- Independent evidence: rechecked raw byte/depth/node/media semantics; equal-high recovery anchor; exact rational versus bounded public rounding; separate-lower-envelope difference and negative-infinity cases; closed capabilities/failures/sizes; source manifest/runtime/canonical artifact identity; BASE/MAX and affected W fixtures. Recomputed W3/W4/W8/W10/W11/W14 and MAX counts without a production evaluator. No B1-B6 blocker remains.
- Exact recalculations: W8 correlated wealth 500k/2m with mean annual log0, offsetting wealth1.25m and `ln(1.25)/7`; W11 separate-envelope difference `+ln(2)/7` rather than paired minimum `-ln(2)/7`; three public 40-digit thirds sum `1-1e-40` while private rationals sum1; MAX has160 PathResults,13,760 alternative events,1,700 benchmark events and53,760 asset-month-alternative evaluations.
- W disposition: every W1-W19 item is closed for design freeze. This means the acceptance oracle is specific, not that any implementation test has run or passed.
- Freeze conditions: Astra must pin exact Policy/design/review hashes; freeze only parser/Bundle, synthetic ledger/timeline/recovery/ruin, finite-member diagnostic, Decimal/display, closed output/failure, canonical provenance, stateless API/CLI/UI, allowed files and W1-W19. Preserve all prohibited files and no-decision/no-provider/no-write boundaries. Any material semantic change reopens review.
- First next action: Astra adds the narrow ADR freeze only. After it exists, Terra implements exactly the frozen files/contracts and returns full focused/regression/compile/JS/startup/hash/diff, isolated no-write HTTP/CLI and real desktop/mobile browser evidence to Sol post-validation.
- Status boundary: no implementation, pytest, DB, provider/network, browser, model/OOS/shadow test or trial occurred in this re-review. No `PASS_ENGINEERING` or Policy §18 item follows. `RISK BUDGET NOT APPROVED`; model/risk/provider/PIT/calibration/OOS `NOT VERIFIED`; `SHADOW VALIDATION NOT PASSED`; `NOT PRODUCTION READY`.
- Verification: Policy/design/review hashes rechecked exactly. `python scripts/verify_startup.py --root .` exited0 with structural PASS, findings[], actual main/UNBORN/dirty and Production denied. `git diff --check` exited0 but has the known all-untracked limitation; explicit inspection found no trailing whitespace in the review/operational files touched by Sol.
- Responsibilities: Sol pre-review complete; Astra freeze pending; Terra undispatched. Application/tests/Policy/ADR/frozen docs/provider state/user DBs/research ledger/Production gate were not changed by Sol.

## 2026-09-14 RESEARCH-WEALTH-LAB-V1 corrected revision 2 — Sol re-review required

- Changed / Why: Astra corrected only `docs/RESEARCH_WEALTH_LAB_DESIGN.md` and START_HERE/HANDOFF/TODO/TEST_STATUS/FILE_MAP for Sol's six bounded blockers. Corrected artifact SHA-256 `4097F897073E681ABAB4B5535EAA81ED38F32F2A9BAE72B49C672B6FFF74B80C`. Previous review remains unchanged at `DAF4A7A18123A9CE136EEC4795CB1C18FA64D60E2AB4811471A4EC2726B30646`; its draft-1 failure is retained historical evidence, not a verdict on revision 2.
- B1: section 2 selects raw-bytes-only public library/HTTP/CLI evaluation; exact depth/node counting, duplicate pointers, media/content encoding and parsing-stage precedence. B2: section 3 fixes event-indexed equality/flat-high anchor behavior and explicit event-kind episode fields; section 8.1 provides exact episode objects.
- B3: sections 3.1/4 separate exact internal rational invariants from rounded public decimals and specify error bounds, exact input probability sums, complete output number grammar, display tie membership and one equation: difference of separately minimized lower envelopes. Section 8.1 includes the sign-reversal and three-thirds counterexamples.
- B4: sections 6–6.2 close capabilities, Evaluation/Artifact success/failure wrappers, unavailable reasons, ordered error stages/aggregation, hash/provenance nullability, file/authority/encoding/output-write failures and exact 32MiB Evaluation/40MiB Artifact canonical byte checks. B5: section 7 fixes source manifest including core/API/CLI/app/JS/CSS/fixture/lock identities, runtime versions, loaded-source checks, canonical JSON, server artifact hashing and independent browser/API/CLI rehash.
- B6: section 8.1 specifies complete BASE/MAX builders, separate valid/invalid W6 cases, exact W8 coupling and W9 recovery results, W11 comparison/W14 rounding oracles, W17 manifest/restart behavior, W19 dimensions/byte-measurement and private fault seams. No public test bypass or broadened model scope.
- Evidence / Verification: full Sol report and actual draft read, selected Policy hash unchanged; frozen editor design and retained reviewer test remain their exact recorded hashes. The revised design has no trailing whitespace. No application test, numerical/model experiment, DB or provider/network operation occurred; fixtures are prospective acceptance specifications. Startup/whitespace results are recorded in TEST_STATUS after checkpoint.
- Status / Limitations: `CORRECTED DRAFT / SOL INDEPENDENT RE-REVIEW REQUIRED / NOT FROZEN`. Astra's correction is not independent blocker closure, implementation acceptance or model validation. No economic forecast/optimizer/risk feasibility/action, current-budget impact, provider approval, personalized or Production behavior was added. Design deviations: NONE intended; explicit synthetic/non-decision subset retained.
- First next action: Sol high independently compare B1-B6 and affected W rows against revision-2 hash above, including parser stage collisions, equality anchors, separate-minimum difference sign, public roundoff, artifact bytes/source context, maximum-dimension outputs and fault envelopes. Return a scoped exact-hash disposition; no ADR freeze or Terra dispatch until required review passes. Do not change Policy, ADRs, application/tests, provider/DB/ledger or Production register during re-review.
- Responsibilities: Astra correction complete, Sol re-review pending, Terra undispatched. Mandatory statuses unchanged: `RISK BUDGET NOT APPROVED`; model/risk feasibility/provider/PIT/calibration/OOS `NOT VERIFIED`; `SHADOW VALIDATION NOT PASSED`; `NOT PRODUCTION READY`.
- Final operational check: startup verifier exit 0 / structural PASS / findings [] / main-UNBORN-dirty / Production denied; git diff whitespace check exit 0 with the documented untracked-file limitation.

## 2026-09-14 RESEARCH-WEALTH-LAB-V1 Sol pre-review — DESIGN CHANGE REQUIRED

- Changed / Why: created `docs/reviews/research_wealth_lab_pre_review.md`, SHA-256 `DAF4A7A18123A9CE136EEC4795CB1C18FA64D60E2AB4811471A4EC2726B30646`, after independently reading the selected Policy and exact draft hash `2DDDC542373193ABAE53165A66AE96151AFDE9FB3662F04B2EBC7F6448492AA3`, reproducing W3/W4/W7/W10/W11 and falsifying the parser/output/recovery/provenance contracts. Updated only START_HERE/HANDOFF/TODO/FILE_MAP/TEST_STATUS alongside the new review; application, tests, Policy, frozen docs/ADRs, provider state, DBs, trials and Production gate are unchanged.
- Verdict: **`DESIGN CHANGE REQUIRED / NOT READY FOR SCOPED DECISION FREEZE`**. Six blockers: B1 impossible raw-duplicate parity for a prebuilt dict plus undefined depth/node/content-encoding boundaries; B2 flat-high recovery anchor ambiguity; B3 40-digit public fractions cannot satisfy exact-sum W14 and `difference_to_no_trade` can reverse sign under two plausible formulas; B4 unclosed capabilities/findings/errors/failure priority/result-size bytes; B5 incomplete artifact-determining source provenance; B6 W1/W6/W8/W9/W17/W19 are not yet exact independent oracles.
- Formula evidence: W3 cash 38/post-trade 98/month-1 110.8; W4 `ln(.98)/7=-0.002886101045359921201149328717741769693...`; W10 expected annual log 0; W11 member utilities `+/-ln(2)/14`. Counterexamples: three exact internal fractions of 1/3 cannot sum to exactly one after separate 40-digit decimal rendering; utilities alt `[0,-1]`, no-trade `[-2,0]` yield +1 for difference of lower envelopes but -1 for worst paired difference; a flat high at month 1 before a month-2 drop yields recovery anchor month 1 or 0 under competing prose readings.
- W disposition: change required W1, W2, W6, W8, W9, W14, W17, W18, W19; ready after a corrected freeze W3-W5, W7, W10-W13, W15-W16, with W11 limited to its member/lower-envelope math.
- Validation boundary: document review only; no application/DB/provider/network/browser/model/OOS test. Narrow synthetic accounting remains a sound next slice. Cost-once, strict ruin, finite supplied lower-envelope terminology, stress separation, benchmark/passive/cash/no-trade distinctions, no-write integration and allowed-file scope are nonblocking.
- Verification: selected Policy and reviewed draft hashes remained exact. `python scripts/verify_startup.py --root .` exited 0 with `startup_integrity=PASS`, findings `[]`, actual main/UNBORN/dirty and `NOT PRODUCTION READY`. `git diff --check` exited 0 but cannot inspect untracked files; the new review and modified operational files were explicitly inspected and the new review has no trailing whitespace. No application test was appropriate for this unimplemented design.
- First next action: Astra reads the review B1-B6, corrects only the draft contract and affected W rows, preserves all non-decision/stateless boundaries, computes a new raw SHA-256 and sends that exact artifact to Sol. No ADR freeze or Terra implementation before a re-review PASS.
- Mandatory state: Decisions remain D0009; no wealth-lab ADR exists. `RISK BUDGET NOT APPROVED`; investment model/risk feasibility/provider/PIT/calibration/OOS `NOT VERIFIED`; `SHADOW VALIDATION NOT PASSED`; `NOT PRODUCTION READY`.

## 2026-09-14 RESEARCH-WEALTH-LAB-V1 design checkpoint

- Changed / Why: Astra created `docs/RESEARCH_WEALTH_LAB_DESIGN.md`, draft 1, SHA-256 `2DDDC542373193ABAE53165A66AE96151AFDE9FB3662F04B2EBC7F6448492AA3`. The smallest proposed next slice is a stateless synthetic joint-path and self-financing wealth laboratory, not a prematurely calibrated forecast/optimizer. Updated START_HERE/HANDOFF/TODO/TEST_STATUS/FILE_MAP only; existing Decisions and frozen artifacts remain unchanged.
- Scope: ADR-0012's fixed synthetic 7Y/USD/nominal/pre-tax/no-flow subset; supplied hypothetical initial holdings and alternatives, one initial cost-bearing trade, unchanged units and distributions-to-cash, exact rational wealth/ruin/drawdown accounting, assumed finite probability-member log-utility arithmetic, separate unweighted stress diagnostics. API/CLI/same-process UI and exportable source/input provenance are proposed; no DB migration/writes, provider data or network.
- Boundaries / Model limitations: no economic/prior forecast, fitted probability, calibrated U sets/margins, risk feasibility, optimizer, ranking or action. The lab does not apply the current edited Risk Budget. Real CRSP/VTI/proxy availability remains unverified. §18 items 1–25 are individually retained as unfulfilled production gates in design section 10.
- Evidence / Tests / Results: Policy raw hash reverified as `ACF013CE46F450423D83DEEBA1C207B22C669670C2B9BDC83A8A665F5AD38E67`; current Policy/ADRs/architecture/integration code and accepted editor/provider boundaries inspected. `git diff --check` reports no tracked whitespace error; repository is UNBORN and files untracked, so this does not verify new-file content. No application test, model experiment, DB access, network call or research-trial record occurred in this design pass. Numerical cases in section 8 are proposed independent-oracle acceptance, not executed results.
- Validation Status: `DRAFT / SOL INDEPENDENT REVIEW REQUIRED / NOT FROZEN`. No new PASS or implementation acceptance. Design Deviations: no intended replacement of Policy semantics; explicit incomplete synthetic subset.
- Operational verification after checkpoint: `python scripts/verify_startup.py --root .` exit 0, structural startup PASS with findings [], actual main/UNBORN/dirty, Production denied. No investment/model validation follows from this result.
- First next action: Sol high independently falsifies the selected Policy requirements, draft sections 2–6 formulas/contracts and section 8 numerical cases before assessing Astra's rationale; write a bounded pre-review under docs/reviews with exact reviewed hash, findings and scoped disposition. Review must challenge costs, strict zero/infinite utility, cash/benchmark distinctions, finite-family terminology, output closure, no-state/no-promotion boundaries and implementation size. Astra resolves material findings; only a subsequent exact-hash scoped ADR freeze can authorize Terra.
- Agent responsibilities: Astra draft complete, independent Sol pre-review pending, Terra not dispatched. Do NOT Change: Policy/frozen designs/ADRs, application/tests during pre-review, defaults, permissions, protected DBs, research ledger or Production register. `RISK BUDGET NOT APPROVED`, numerical/model/provider/PIT/OOS `NOT VERIFIED`, `SHADOW VALIDATION NOT PASSED`, `NOT PRODUCTION READY` remain.

## 2026-09-14 next-slice design dispatch

- `PASS_ENGINEERING_RESEARCH_RISK_EDITOR` is recorded with complete independent evidence below. The next actionable P1 item is forecast/wealth/risk/optimizer/uncertainty-ledger design; provider capture remains separately blocked by permission/source evidence.
- Astra must select and specify the smallest coherent research-only implementation slice that advances this architecture without guessing provider truth, calibration, `U_objective`/`U_risk` radii, `S_stress` limits, residual margins, costs, solver tolerances, risk approvals or Production semantics. It must preserve the selected Policy objective, ADR-0012 provisional mandate, existing fail-closed provider/PIT gates and all editor contracts.
- Required sequence: Astra corrected design draft and acceptance matrix -> Sol independent falsification-first pre-review -> explicit scoped ADR freeze only on PASS -> Terra implementation -> automated evidence -> Sol post-validation. No code is authorized from the draft alone.
- First next action: Astra reads the selected Policy, current Decisions/TODO, initial design, schema/services/tests and accepted editor/provider boundaries, then creates a newcomer-ready design artifact with formulas, closed inputs/outputs, data flow, invariants, failure statuses, migration/API/CLI/UI scope, deterministic synthetic acceptance evidence and explicit deferred calibration/OOS/provider work.
- Status boundaries remain `RISK BUDGET NOT APPROVED`; numerical feasibility/impact/calibration, provider/PIT/model/OOS `NOT VERIFIED`; `SHADOW VALIDATION NOT PASSED`; `NOT PRODUCTION READY`.

## 2026-09-13 Sol final U1-U3 recheck — scoped engineering PASS

- Final independent verdict: **`PASS_ENGINEERING_RESEARCH_RISK_EDITOR`** for the frozen SQLite/API/CLI/same-process-editor engineering contract. The controlling report is `docs/reviews/research_risk_editor_post_review.md` section 13, SHA-256 `6B49E8FF640B7648A9875F7572B205D5FA89B9241A6E935406B4580A22E55CB3`.
- Authority/identity: Policy `ACF013CE46F450423D83DEEBA1C207B22C669670C2B9BDC83A8A665F5AD38E67`; frozen design `F9EC05E7F96A3294FD819CFB53C99B9FC818B5CE9C86C96C69E701910E44B950`; retained reviewer test remained byte-identical at `EF9E02011A3B66249B84CFC0BD5654E5A3571CCC314D8E4E21C40143C2AB799B`.
- U1: independent exhaustive mutation testing rejected reserved `NOT_APPLICABLE` on all 30 applicable Risk/Stress/Hybrid wrapper cases in direct validation and the same 30 API cases; a nonreserved incomplete reason remained valid. U2/U3: initial HTML has 19 genuine visible labelled readonly controls; hydration removes them and renders 19 structured rows plus two row-16 components with no editable aggregate; current V1 name/source are explicit.
- Real browser: desktop and 390x844 mobile had no horizontal overflow; mobile effective `clientWidth=scrollWidth=375` with zero overflow offenders. Edit, clear/reason, preview, provisional save, reload, history, stale-draft preservation, XSS safety and provider-blocked isolation passed. With `NOT_VERIFIED` provider declarations, editor HTTP 200 remained available while home/datasets were HTTP 403 `BLOCKED_POLICY`.
- Automation: focused editor/reviewer **63 passed, 2 warnings**; full suite **135 passed, 2 skipped, 2 warnings**. Skips remain host-denied Windows symlink cases. Python compile, JavaScript syntax, startup checker and `git diff --check` passed; startup remained structural-only `NOT PRODUCTION READY`.
- Cleanup: `data/browser_u3_20260913.db` was confirmed as the disposable review artifact by creation/helper identity and matching current budget ID. Its lingering local port-8768 helper was stopped and the DB removed (`ExistsAfter=False`). `phase2_check.db`, `optivest.db` and user/protected databases were untouched.
- First next action: proceed only from `.ai/TODO.md`; numerical feasibility/impact/calibration and forecast/wealth/risk/optimizer design remain open, and provider capture remains separately blocked pending permission/source work.
- Mandatory boundaries: `RISK BUDGET NOT APPROVED`; numerical feasibility/impact/calibration, provider/PIT/model/OOS `NOT VERIFIED`; `SHADOW VALIDATION NOT PASSED`; `NOT PRODUCTION READY`. Design deviations: NONE.

## 2026-09-13 Terra U1-U3 correction checkpoint — Sol recheck required

- Controlling verdict remains **`IMPLEMENTATION VALIDATION FAILED / REVIEW_REQUIRED`** pending one final independent Sol recheck. Do not set `PASS_ENGINEERING_RESEARCH_RISK_EDITOR` from this Terra evidence.
- Changed only `optivest/risk_editor.py`, `optivest/service.py`, `optivest/app.py`, `optivest/static/risk_editor.js`, `optivest/static/risk_editor.css`, `tests/test_risk_editor.py`, and the `.ai` operational records. Policy/design/ADR, retained independent tests, defaults, provider permissions and readiness semantics are unchanged.
- U1 root cause was that applicable selected-mode wrappers received generic null-reason validation while only forbidden wrappers required exact `NOT_APPLICABLE`. `_require_applicable_m` now rejects the reserved reason for required Risk fields, selected Stress fields and applicable Hybrid fields. Permanent direct-schema and API tests cover the three Sol candidates.
- U2/U3 implementation: CSS gives headings, row/component containers, grids, pairs and inputs wrap/shrink constraints; status exposes persisted V1 `risk_budget_name` and `source_preset`; the current summary displays both. The initial HTML now has 19 visible labelled readonly row placeholders (and rationale), clearly explaining that choosing/reloading hydrates live structured controls; JS removes those placeholders when it renders the 19 editable rows. No hidden compatibility anchors or independent-test edits were used.
- Terra evidence: focused editor + retained independent suites **63 passed, 2 warnings**; Python compile, `node --check`, startup checker and diff whitespace passed. Isolated local 390px browser probe after Balanced hydration: 19 rows; `scrollWidth=375` equals `clientWidth=375`; current name/preset present; initial placeholders removed. The disposable DB is `data/browser_u3_20260913.db`; do not use it as evidence beyond this UI probe.
- First next action: Sol independently verify retained test SHA-256, run focused and full suites, inspect the U1 mutation order/semantics, and repeat the actual 390px browser matrix including current metadata. If all pass, issue only scoped engineering disposition; retain all policy/model/readiness limitations.
- Boundaries unchanged: `RISK BUDGET NOT APPROVED`; feasibility/impact/calibration, provider/PIT/model/OOS `NOT VERIFIED`; `SHADOW VALIDATION NOT PASSED`; `NOT PRODUCTION READY`. Design deviations: NONE identified.

## 2026-09-13 Sol independent revalidation disposition

- Controlling verdict remains **`IMPLEMENTATION VALIDATION FAILED / REVIEW_REQUIRED`**; do not set `PASS_ENGINEERING_RESEARCH_RISK_EDITOR`. Exact item-by-item evidence and bounded U1–U3 findings are in `docs/reviews/research_risk_editor_post_review.md` section 12, SHA-256 `3ABD66BB35D16E4B5BE9B038C3D386AD73E6171DB15B65BE539BCB957C6644BF`.
- Closed independently: populated `0003→0002→0003` preserves 2 budgets/38 constraints/2 providers and reclassifies the stripped head as `LEGACY_NOT_VERIFIED`; reversed historical arrays, an extra marker column and rehashed historical `LEVERAGE=1` all block startup/status/preflight with no snapshot; preview emits explicit `BEGIN`, is row-count read-only, and exposes complete legacy diagnostics/UTC `Z`; two-client CAS is one 201/one 409.
- Open U1: applicable `ROBUST_CHANCE.epsilon`, selected `BINDING.version`, and Hybrid observed-component wrappers each accept `{value:null,reason:"NOT_APPLICABLE"}`. Enforce the frozen reserved-reason invariant centrally and add permanent API/schema regressions.
- Open U2: the real structured UI renders 19 rows and both row-16 components and passes desktop edit/clear/preview/save/reload/history/stale/XSS. At 390x844 it horizontally overflows because long metric headings do not wrap; the current summary also omits explicit current name/source preset. Correct responsive wrapping and current metadata, then repeat real-browser acceptance.
- Open U3: retained reviewer test line 570 inspects initial HTML and fails `1 >= 19` because controls are created by deferred JavaScript. Live browser proves controls exist. Reconcile this contract without hidden anchors or silently weakening/deleting independent coverage; any reviewer-test change requires independent approval.
- Exact automation: focused editor/reviewer **1 failed, 59 passed, 2 warnings**; full suite **1 failed, 131 passed, 2 skipped, 2 warnings**. Same retained UI assertion fails both. Python compile, JS syntax, startup checker and `git diff --check` pass; startup remains structural-only `NOT PRODUCTION READY`.
- First next action: Terra reads frozen design section 2/10/11 and post-review section 12, implements only U1–U3, adds bounded regressions, and returns to Sol. Do not alter Policy/design/ADR/default values/provider permissions or any approval/readiness state.
- No user/real database, network/provider capture, deployment, commit or subagent was used. Disposable browser server was stopped. One disposable browser directory may remain under the OS temp directory because deletion was denied by the execution policy; it is outside the repository and contains only the isolated review DB/logs.
- Boundaries unchanged: numerical feasibility/impact/calibration, provider/PIT/model/OOS remain `NOT VERIFIED`; `RISK BUDGET NOT APPROVED`, `SHADOW VALIDATION NOT PASSED`, and `NOT PRODUCTION READY` remain mandatory. Design deviations: NONE identified in the intended investment semantics; implementation conformance remains failed.

## 2026-09-13 resume and independent-validation dispatch checkpoint

- The selected Policy was fully reread and its raw SHA-256 reverified as `ACF013CE46F450423D83DEEBA1C207B22C669670C2B9BDC83A8A665F5AD38E67`, matching `.ai/START_HERE.md`; no `POLICY VERSION CHANGED` condition exists.
- Repository identity remains `main` / `UNBORN`. All project entries remain untracked, so preserve the existing tree and do not infer a clean checkout. Current design SHA-256 remains frozen at `F9EC05E7F96A3294FD819CFB53C99B9FC818B5CE9C86C96C69E701910E44B950`; the retained independent test remains byte-identical at `EF9E02011A3B66249B84CFC0BD5654E5A3571CCC314D8E4E21C40143C2AB799B`.
- Current corrected implementation hashes are `risk_editor.py` `FACFBA7C01D2C75E80A021B895F6077A72C053F98199E9F16E5667D37841DFF2`, `service.py` `515EEFBF8794A1B24AC115F760098C3CC9EAC5C1026CFCC4FFC24E5D323B3386`, `app.py` `D5BBBF6C1B3F8C7E548135168A258C3569C237791D4B16E546C08DA8C4C648CA`, and `test_risk_editor.py` `A90882806ECB58C578F1C151354489E9475DC39EE2BA8B844758675A50B75C1D`; these differ from the failed section-11 review because the bounded T1-T4 correction is present.
- First next action: Sol independently validates that correction against frozen ADR-0017/design section 11, including the populated 0003-to-0002-to-0003 comparator roundtrip, canonical-history/extra-column/historical-contradiction probes, explicit preview read transaction, desktop/mobile structured-control browser acceptance, full regression, and operational-state review. If a blocking implementation defect remains, return an exact bounded finding for one Terra fix pass; otherwise record scoped engineering acceptance only.
- Current status remains `IMPLEMENTATION VALIDATION FAILED / REVIEW_REQUIRED` until that independent recheck finishes. Numerical feasibility/impact/calibration, provider/PIT/model/OOS remain `NOT VERIFIED`; `RISK BUDGET NOT APPROVED`, `SHADOW VALIDATION NOT PASSED`, and `NOT PRODUCTION READY` remain unchanged. Design deviations: NONE identified at dispatch.

## 2026-09-10 Terra bounded T1–T4 correction checkpoint

- Sol independently reproduced the updated implementer suite: `uv run pytest -q tests\test_risk_editor.py --tb=line` **31 passed, 2 warnings**. `uv --cache-dir .uv-cache run python scripts\verify_startup.py --root .`, `node --check optivest\static\risk_editor.js`, and `git diff --check` passed. An initial startup invocation without `--cache-dir .uv-cache` hit the documented host uv-cache permission error; the scoped-cache rerun passed. This is partial verification only and does not close the remaining acceptance work below.
- Policy hash and frozen ADR/design are unchanged. No design change is required for the reproduced section-11 findings.
- Implemented in `optivest/risk_editor.py`, `service.py` and `app.py`: closed positive/range/applicability/text/ID grammar; finding-backed deterministic blockers and Joint horizon contradiction; canonical-history and exact-column/FK verification; historical contradiction rejection; post-downgrade `<=`/`>=` legacy comparators; UTC `Z` timestamps; complete legacy diagnostics; explicit preview read transaction; no-space UTF-8 media type, raw encoding and top-level missing-version/error envelopes.
- Replaced the unreachable/raw-JSON dual UI with one reachable structured editor plus `optivest/static/risk_editor.js` and `.css`. It renders all rows and M wrappers, structured horizons, risk/stress/hybrid state, structured Joint groups, and grouped TURNOVER/IMPLEMENTATION_COST forms while row 16 aggregate is non-editable. It preserves draft on conflicts, requires preview before save, disables duplicate submissions, uses DOM `textContent`/properties for free text, and exposes history/binding/unavailable personalized panels.
- Permanent tests in `tests/test_risk_editor.py` cover the reproduced T1 value grammar, trimming, finding/blocker derivation/order, Joint contradiction, API typing/encoding/media/missing-version/Z timestamp and reachable non-textarea asset surface.
- Evidence: pre-UI editor+retained suite **51 passed**; final implementer-focused suite **31 passed**; Python and JavaScript syntax, startup checker, Policy hash and tracked diff whitespace passed. Full suite, populated 0003→0002→0003 comparator roundtrip, direct SQL-trace assertion and real-browser recheck were intentionally not run in this quota-bounded checkpoint and remain `NOT VERIFIED` pending Sol/next agent.
- First next action: independently run the new and retained editor suites, then reproduce T3 canonical-array/extra-column/historical-contradiction probes and the populated migration roundtrip. If green, perform desktop/mobile structured-control browser acceptance and full regression. Do not infer acceptance from this Terra checkpoint.
- Status boundaries are unchanged: `IMPLEMENTATION VALIDATION FAILED / REVIEW_REQUIRED`; `RISK BUDGET NOT APPROVED`; feasibility/impact/provider/PIT/model/OOS `NOT VERIFIED`; `SHADOW VALIDATION NOT PASSED`; `NOT PRODUCTION READY`. Design deviations: NONE identified; independent post-validation is mandatory.

## 2026-09-10 final Sol revalidation after third correction

- Controlling verdict remains **`IMPLEMENTATION VALIDATION FAILED / REVIEW_REQUIRED`**. Do not set `PASS_ENGINEERING_RESEARCH_RISK_EDITOR`. Exact details and the 11-item disposition are in the latest section of `docs/reviews/research_risk_editor_post_review.md`.
- The retained independent test remains byte-identical at `EF9E...799B`; focused editor/reviewer tests pass 51/51 and the full suite passes 123 with 2 host-denied symlink skips. Compile, startup checker and tracked `git diff --check` pass. Green automation does not override direct counterexamples.
- Open frozen grammar/API defects: zero positive-duration and component percent 101 are accepted; exact inapplicable/fixed row-16/text trimming/identifier/request typing is incomplete; bad UTF-8 maps to the wrong code; valid no-space UTF-8 Content-Type is rejected; timestamps lack `Z`; legacy diagnostics are incomplete.
- Open evaluator defects: a fully populated 6Y Joint group over 7Y members is not contradictory; mandatory blocker codes are absent from `findings`; catalog ordering is lexical; the deterministic diagnostic contract is incomplete.
- Open persistence defects: rehashed unsorted historical V1 arrays, an extra marker-table column and a synchronized historical `LEVERAGE=1` contradiction all verify `VALID`; the corrupt historical DB serves and preflight inserts. Populated V1 0003→0002→0003 now fails because the legacy verifier rejects preserved V1 `>=` comparators.
- Preview performs 19 SQL statements without explicit `BEGIN`. Write CAS/fault/busy cases remain passing.
- Actual in-app browser validation was completed on an isolated provider-blocked DB: desktop/mobile edit/clear/preview/save/reload/history/stale/XSS mechanics pass and the editor remains available while provider/home surfaces are 403. UI conformance still fails because the served editor is a raw JSON textarea plus hidden anchors, not the frozen structured row/component editor; row 16 is not grouped and its aggregate is editable.
- First next action: Terra performs one bounded correction for the exact T1–T4 and UI findings, adds permanent tests including the populated downgrade/re-upgrade regression, then Sol rechecks all section 11 items. Do not alter the frozen design/ADR or any numerical/provider/readiness state.
- Review environment incident: a failed PowerShell path expression caused idempotent seed commands to open ignored `optivest.db`. Read-only inspection found original 2026-09-08 logical rows/counts and no permission/status/value change, but file mtime advanced. No destructive rollback was attempted.
- Current limits at review end: about 27% of the 5-hour window and 10% of the weekly window remain. Continuity records were updated before more work.

## 2026-09-10 resume checkpoint before correction

- The selected Policy was re-hashed at `ACF013CE46F450423D83DEEBA1C207B22C669670C2B9BDC83A8A665F5AD38E67` and fully re-read (841 lines). It matches START_HERE; no Policy-version change is present. Git remains `main` / `UNBORN`; all project files are untracked, so preserve the existing tree and do not infer a clean checkout.
- Current Codex account usage reports about 22% of the weekly window remaining. Per continuity policy, this handoff was refreshed before further implementation.
- Resume scope is exactly the open second-recheck findings in `docs/reviews/research_risk_editor_post_review.md`: recursive closed grammar/canonical rules; required evaluator and directional/identity-aware difference semantics; exact schema/history/legacy-chain verification and fail-closed serving; then the functional `/risk-budget` editor and mandatory desktop/mobile browser acceptance. Do not change the frozen design, Policy, provisional numerical values, permissions, or Production status.
- Ownership: Terra performs one bounded correction against ADR-0017 and adds permanent regression coverage; Sol then independently falsifies the result. If implementation requires a semantic change, return `DESIGN CHANGE REQUIRED` instead of modifying the frozen contract.

## Current authoritative checkpoint — independent editor post-validation

- Sol completed the independent second-correction recheck against ADR-0017/design SHA-256 `F9EC05E7F96A3294FD819CFB53C99B9FC818B5CE9C86C96C69E701910E44B950`. Verdict remains **`IMPLEMENTATION VALIDATION FAILED / REVIEW_REQUIRED`**. Do not set `PASS_ENGINEERING_RESEARCH_RISK_EDITOR`.
- Updated report: `docs/reviews/research_risk_editor_post_review.md`, SHA-256 `BA1FA18499BE421C77FDB8EEABB9DDEBAD87873928949B939C1F73BDA30F8368`. The retained reviewer test remains unchanged at `EF9E...799B` and passes 29/29; the 11 editor tests pass, including permanent coverage for all nine prior HTTP-200 cases.
- B6 is now closed for the exercised transaction contract: concurrent saves yielded one 201 and one 409 `HEAD_CONFLICT`; mandate movement also returns `HEAD_CONFLICT`; preview includes both observed heads; marker/serialization/busy faults remain atomic.
- Exact verification remains fail-open. A legacy `UNKNOWN_METRIC`, a same-name no-op immutability trigger, and a historical V1 envelope missing `created_at` plus an unknown server key with recomputed digest all returned `VALID`. The last database also passed startup/status/current/history and inserted a preflight snapshot. Root-revision and raw digest-mismatch corruptions now reject.
- Frozen grammar/evaluator/difference cases remain open: wrong row/component event clocks, invalid currency applicability, blank/numeric text, duplicate Joint group IDs/cross-group member reuse and unsorted Joint members are accepted; selected incomplete risk/stress/joint findings are missing; `>=`/minimum/identity-aware comparisons are wrong.
- `/risk-budget` has 21 inputs and a listener/history request, but only row limits are wired; component inputs and the required class/unit/reason/horizon/mode/joint/binding/difference state are absent. Browser discovery returned `[]`; frozen §11 item 9 makes the unperformed real desktop/mobile interaction a blocking `NOT VERIFIED`, not optional polish.
- Checks: retained reviewer **29 passed**; editor **11 passed**; full suite **112 passed, 2 skipped**; compile/startup checker/diff whitespace PASS. Fresh populated 0003→0002→0003 preserved 2 budgets/38 constraints/1 provider and reclassified the document-stripped head as legacy. CLI show/preview/save/verify succeeded on that disposable DB. These green results do not override direct counterexamples.
- Latest disposition: B3 and independently exercised B6 closed; B1, B2, B4, B5 and B8 open/blocking; B7 partial/blocking. First next action: Terra completes the frozen recursive grammar/evaluator/change engine and exact all-history/schema verifier, then the functional editor UI; add permanent tests for every reproduced case and return to Sol with an available browser session for mandatory §11 item 9.
- No application/test fix, Policy/design/ADR/default risk/status/provider-permission change, network call, protected DB access, commit or subagent occurred. `RISK BUDGET NOT APPROVED`, feasibility/impact/provider/PIT/model/OOS `NOT VERIFIED`, `SHADOW VALIDATION NOT PASSED`, and `NOT PRODUCTION READY` remain unchanged. Scope validation is not numerical risk validation.

## Historical freeze checkpoint — research Risk Budget editor

- Astra verified selected Policy/design/review hashes and froze corrected revision 2 as **ADR-0017 / RESEARCH-RISK-EDITOR-V1**, decision version D0009. The reviewed design retains historical draft wording to preserve its hash; ADR-0017 controls current implementation authorization.
- This independent offline slice addresses the currently static risk UI and default-reset successor behavior. It proposes immutable complete research declarations, editing/preview/history, input consistency and explicit `NOT VERIFIED` feasibility/impact. Full numerical §8 feasibility/sensitivity remains unfinished until separately designed risk/wealth/optimizer inputs exist.
- Next exact step: Terra reads ADR-0017 and the exact frozen design, then implements its closed schema/catalog/parser/evaluator, marker/document migration and version service before API/CLI/UI integration and all section 11 acceptance checks. Preserve existing provider gating; use isolated databases only. Independent Sol post-validation follows implementation/checks.
- Changed files in this freeze checkpoint: DECISIONS, START_HERE, HANDOFF and FILE_MAP only. The reviewed design and review bytes are unchanged. No Policy, application, DB, permission, actual numerical risk setting or validation status changed. Exact design SHA-256 `F9EC05E7F96A3294FD819CFB53C99B9FC818B5CE9C86C96C69E701910E44B950`; review SHA-256 `EA061F0994AAF176515782CDFE231CA96D7D462F2BC3BE69596CD426FE3AFCF7`.
- Ownership: Sol pre-review and Astra freeze complete; Terra implementation next; Sol independent post-validation pending. `RISK BUDGET NOT APPROVED`, feasibility/impact/provider/PIT/model/OOS `NOT VERIFIED`, `SHADOW VALIDATION NOT PASSED` and `NOT PRODUCTION READY` remain unchanged. Earlier draft/failed-review checkpoints below are historical evidence, not current authorization state. Full numerical feasibility/impact TODO remains open.
- Corrected design SHA-256: `F9EC05E7F96A3294FD819CFB53C99B9FC818B5CE9C86C96C69E701910E44B950`. `uv run python scripts/verify_startup.py --root .` passed structural checks with main/UNBORN and unchanged Policy hash; `git diff --check` passed (untracked files are outside Git diff coverage). No application test or investment validation was run.

## 2026-09-09 independent research Risk Budget editor pre-review

- Sol independently read the selected Policy and current schema/service/API/CLI/UI/tests before comparing `RESEARCH-RISK-EDITOR-V1`. Report: `docs/reviews/research_risk_editor_pre_review.md`; reviewed draft SHA-256 `5E275D4095A20886193771C1E645AE4765FC5BA6A0E5F8D22CAE427082F10883`.
- Verdict: **`DESIGN CHANGE REQUIRED / NOT READY FOR DECISION FREEZE`**. Scope separation is acceptable, but eight blockers remain: normative closed schema/catalog/projection; structured horizon/event-clock semantics; raw duplicate-key-safe JSON boundary; class-by-semantics matrix; deterministic evaluator/status contract; atomic mandate+risk two-head CAS; exact legacy/V1 transition behavior; and complete DB tamper/revision verification.
- Next exact step: Astra corrects B1-B8 in the review, recalculates the design hash and requests a new independent pre-review. Do not freeze or begin Terra implementation until that review passes. No Policy/objective/mandate/risk value/status/provider permission/application code changed; no network or tests were used for this document-only pre-review.
- Ownership: Astra revision required; Sol re-review pending; Terra not started. Scope acceptance is not numerical feasibility, calibration or risk validation. `RISK BUDGET NOT APPROVED`, feasibility/impact/provider/PIT/model/OOS `NOT VERIFIED`, `SHADOW VALIDATION NOT PASSED` and `NOT PRODUCTION READY` remain unchanged.

## 2026-09-09 revision 2 independent pre-review disposition

- Corrected design SHA-256 `F9EC05E7F96A3294FD819CFB53C99B9FC818B5CE9C86C96C69E701910E44B950`; Policy SHA-256 unchanged at `ACF013CE46F450423D83DEEBA1C207B22C669670C2B9BDC83A8A665F5AD38E67`.
- Verdict: **`PASS FOR SCOPED DECISION FREEZE`**. B1-B8 are each closed; no new material blocker was found. Exact closure and nonblocking implementation conditions are appended to `docs/reviews/research_risk_editor_pre_review.md`.
- Scope boundary: this accepts the editor design only. Numerical feasibility/calibration, `U_risk`, `S_stress`, model impact, provider/PIT, OOS, shadow, Production approval and every applicable §18 gate remain unverified/unpassed. No implementation, freeze, test, network call, permission/status/value change or commit occurred in the re-review.

## Task / Why
Reconcile the failed Phase 2 post-review against frozen ADR-0015 without changing investment, license, provider-validation or Production semantics. Existing COMPACT Policy file remains non-authoritative.

## Changed / Completed
- Full selected Policy read and exact copy into repository; verified SHA-256 in START_HERE.
- Initialized local Git main; preserved original user files.
- Created AGENTS, required .ai records, initial design, provisional research risk reference and exact §18 evidence register.
- Designed, pre-reviewed, froze, implemented, corrected and independently validated PHASE1-V1 across SQLite/Alembic, domain services, FastAPI, CLI, UI and tests.
- Remediated the current Phase 2 implementation: one frozen-scope `0002_phase2` migration; exact parser artifact registry/replay; CIK binding; paired current-directory dispatch; capture/replay permission gates; durable started/terminal run audit; retrieval-to-snapshot linkage; redirect/HTTP-error audit; strict source-time envelope; scoped immutable SQLite triggers; read-only Phase 2 surfaces and regressions.
- Corrected an interrupted collection-flow syntax/control-flow defect: a paired Nasdaq run now processes both parts before snapshot/head publication, and a failed second part cannot publish a partial head.

## In Progress / Next exact step
Phase 0, PHASE1-V1 and the PHASE2-V1 offline/fixture/SQLite engineering scope are complete. ADR-0016 and `docs/reviews/phase2_remediation_validation.md` record independent falsification closure. First next step: review exact current publisher terms/licensing evidence for SEC and Nasdaq required uses (network capture, local raw storage, retention, derived storage, internal display, redistribution and commercial/Production use). Do not change a permission from `NOT_VERIFIED` without immutable reviewed evidence. Only if every operation needed by the bounded test is `ALLOWED` may design section 7 item 11 run. Do not start forecasts or infer PIT safety from declarations.

## Required files / Changed files
Read selected OPTIVEST_AI_POLICY_V10.md → AGENTS → START_HERE → this file → TODO → ADR-0015 → frozen Phase 2 design → failed post-review → current migration/service/tests. Changed files are enumerated in FILE_MAP; all project files remain uncommitted.

## Do NOT Change
Policy bytes or objective, user-selected authority, original historical files, unapproved risk settings/mandate, production approval flags, existing research trial records. No legacy model assumptions. No code before required scoped freeze.

## Evidence / Tests / Commands
Policy evidence and Phase 0 hashes are in ADR-0011; Phase 1 in ADR-0014; Phase 2 freeze in ADR-0015.
Current remediation: `uv run pytest -q` PASS (53 passed, 2 skipped, 2 dependency warnings); `uv run python -m py_compile optivest\\phase2.py alembic\\versions\\0002_phase2.py` PASS; `git diff --check` PASS. Fresh isolated SQLite DB: CLI upgrade → seed → verify exact `0002_phase2`; programmatic Alembic `downgrade 0001_phase1` → `upgrade head`; reseed → verify exact head/schema/raw integrity PASS. The direct `uv run alembic` entrypoint cannot import the local package under this host, so the equivalent Alembic API invocation was used for the scoped downgrade/upgrade check.
Complete matrix: TEST_STATUS. Provider/PIT/model/OOS/shadow and bounded-live tests not run.

## Results / Validation Status
Phase 0 structural, PHASE1-V1 engineering and PHASE2-V1 offline engineering validation passed. Full Phase 2 acceptance remains `NOT VERIFIED` because bounded official-source capture/replay is `BLOCKED_POLICY`. Mandate is provisional Generic Research; `RISK BUDGET NOT APPROVED`; provider/PIT/model/OOS are `NOT VERIFIED`; `SHADOW VALIDATION NOT PASSED`; `NOT PRODUCTION READY`. No investment validation has passed. PRODUCTION_GATE remains 25 NOT VERIFIED.

## Remaining Issues / Open questions
The research Mandate is an Astra-selected reversible assumption, not Personalized approval. Required license-use permissions remain `NOT_VERIFIED`, so design §7 bounded SEC/Nasdaq live capture and official-byte replay are `BLOCKED_POLICY`; do not alter permissions or contact external sources without evidence/authority. The current source-time envelope preserves claimed values or null/reason and keeps validated availability null; it does not establish PIT. PostgreSQL, calendar/tradability, historical universe/delisting policy, calibration, solver, OOS and shadow remain future work. Direct symlink tests remain skipped because host privilege denies creation; no inference of coverage is permitted.

## Model Limitations / Design Deviations
No forecast model, runtime portfolio engine, measured expected performance, calibrated numerical limits or real forward outcomes exist. SQLite evidence does not establish PostgreSQL behavior. Manual provider declarations cannot establish PIT truth. LONG-HORIZON VALIDATION LIMITATION must remain explicit when forecasts are introduced. Design deviations: NONE from ADR-0015; the migration is consolidated as its one authorized Phase 2 revision.

## Next model-effort / Review / Resume
The Astra publisher license/terms disposition and Sol review are in `docs/PHASE2_LICENSE_EVIDENCE_REVIEW.md` and `docs/reviews/phase2_license_evidence_independent_review.md`. SEC research uses are supportable in evidence, but no permission was frozen because exact reviewed bytes/hash provenance is incomplete. Nasdaq's exception does not unambiguously cover `otherlisted.txt`, so the paired capture remains blocked. Next, obtain unambiguous publisher permission or design/review a replacement forward-universe source. Paid access, license acceptance or billing remains `USER_CONFIRMATION_REQUIRED`. Offline engineering acceptance does not establish provider/PIT/model/OOS/Production validation. Follow §17 hash gate before resume.

## 2026-09-09 license evidence design checkpoint

- Current SEC primary sources explicitly document programmatic EDGAR access, fair-access limits and public copying/further distribution; Sol supports the research mapping. Citation is publisher-requested and an OptiVest application duty, not a permission condition. No database permission changed because immutable source-byte/hash provenance and a freeze remain incomplete.
- Nasdaq Trader explicitly exempts named Nasdaq Events Data including Nasdaq Listed from further licensing, but does not unambiguously name the required `otherlisted.txt` half of the paired dataset. General Nasdaq terms otherwise restrict automated capture, storage and derivative/data-analysis use.
- No permission row changed. No bounded live request was made. Next exact action is unambiguous Nasdaq permission evidence or a separately designed/reviewed replacement source; ambiguity remains `NOT_VERIFIED`.

## 2026-09-09 independent offline acceptance

- Sol's successive fresh-DB falsification passes closed the original P0-1..P0-6/P1-1..P1-3 implementation defects and the later trusted-declaration forgery.
- Final independent declaration probe changed SEC `authority_tier` to `FAKE_TIER`; verifier returned `seed_state=false` and app startup rejected it. Targeted suite: 7 passed.
- Parent reproduced `uv run pytest -q`: **70 passed, 2 skipped, 2 warnings**; compile, `git diff --check`, startup checker and Policy hash passed.
- Verdict: `PASS_ENGINEERING_P2_OFFLINE`. Full Phase 2 acceptance remains unverified because design section 7 bounded official-source capture and replay has not run and is `BLOCKED_POLICY` under current `NOT_VERIFIED` permissions.

## 2026-09-09 review-fix loop

- Added fail-closed review fixes for named non-common Nasdaq instruments, declared parser artifact matching, raw-integrity terminal mapping, collector review status and forward-only review-head movement, plus INTERNAL_DISPLAY protection on the principal Phase 2 evidence endpoints.
- Verified compile, 53 passing tests, two host-permission symlink skips and clean diff whitespace. No live collection occurred; permission states remain `NOT_VERIFIED`/`BLOCKED_POLICY`.
- `C:\AI\App_Dev\OPTIVEST\optivest.db` exists and is an ignored workspace DB modified during review time. Its path was confirmed exactly; it was not removed in this pass because the available execution policy rejected the explicit PowerShell deletion command. Do not touch `phase2_check.db`.

## 2026-09-09 seven-defect Terra correction checkpoint

### Changed / Why

- `optivest/phase2.py`: Nasdaq candidates now require explicit publisher common-stock/ordinary-share evidence; ambiguous, unit, warrant, preferred, structured and fund rows remain `REVIEW_REQUIRED`. The fixture parser exception is removed: collection requires exact stored parser semantic version, name and artifact hash. Retention requires a nonblank duty when allowed; RFC HTTP-date `Retry-After` and redirect-limit evidence are retained.
- `alembic/versions/0002_phase2.py`: dataset heads require a same-scope successful terminal run/result and nonempty parts/rows, closing the STARTED/zero-part bypass. Snapshot usable time advances once during the atomic terminal/head publication transaction.
- `optivest/app.py`: all Phase 2 evidence endpoints and the Data Evidence home view require `INTERNAL_DISPLAY=ALLOWED` before rendering. Once Phase 2 datasets exist, startup requires the exact two-dataset/seven-use matrix; an empty Phase-1-only database remains usable.
- `tests/test_phase2.py`: added direct regressions for every named remaining defect; fixtures declare actual parser hashes and retention duty.

### Evidence / Tests / Results

- Policy hash unchanged: `ACF013CE46F450423D83DEEBA1C207B22C669670C2B9BDC83A8A665F5AD38E67`.
- `uv run pytest -q`: **60 passed, 2 skipped, 2 upstream warnings**.
- `python -m py_compile optivest\\phase2.py optivest\\app.py alembic\\versions\\0002_phase2.py`: PASS; `git diff --check`: PASS.
- Fresh temp SQLite `upgrade -> seed -> downgrade 0001 -> upgrade -> reseed -> verify`: PASS at `0002_phase2`, schema/seed/raw integrity true.

### Validation status / Remaining issue

Phase 2 remains **`REVIEW_REQUIRED`** pending independent Sol recheck. Live SEC/Nasdaq capture remains `BLOCKED_POLICY`; provider/PIT are `NOT VERIFIED`; `RISK BUDGET NOT APPROVED`, OOS/shadow `NOT VERIFIED`, and `NOT PRODUCTION READY` are unchanged. No unavoidable design blocker was found; fixture evidence is not live/provider/PIT acceptance.

## 2026-09-09 final three-open-finding correction

### Changed / Why

- `optivest/phase2.py`: added separate research-only SEC filing-header and XBRL evidence-part parsers. Captured parts retain raw-linked accession, form, filing/report/acceptance fields, primary document and amendment status, plus XBRL filed/frame/context/unit/dimensions when present. The submissions parser still explicitly records that XBRL is not present in that payload; none of these rows are decision-eligible.
- `optivest/phase2.py`: a new snapshot obtains one publication timestamp after its parts and normalized rows exist. That exact value is used for `SourceSnapshot.usable_at`, staged events, terminal run completion, run-result publication and scoped-head publication in the same transaction.
- `optivest/phase2.py`: `trusted_phase2_declarations()` is the single frozen declaration specification. Seed and verification derive parser hashes from the current parser artifact and verify exact provider/dataset IDs/names, endpoints, semantic version, parser name/hash, license linkage/source URL, `NOT_VERIFIED` validation/timestamp states and seven use rows. Name-shaped but incorrect declarations now fail startup/CLI verification.
- `tests/test_phase2.py`: added offline representative SEC filing-header/XBRL raw-byte lineage, missing/malformed parser, exact canonical-time and fake-declaration/startup regressions.

### Evidence / Tests / Results

- `uv run python -m py_compile optivest\\phase2.py optivest\\app.py optivest\\cli.py tests\\test_phase2.py`: PASS.
- `uv run pytest -q`: **64 passed, 2 skipped, 2 upstream warnings**. The skips remain Windows host-denied symlink fixtures and do not establish link coverage.
- `git diff --check`: PASS.
- Fresh isolated SQLite: Alembic `upgrade head -> downgrade 0001_phase1 -> upgrade head`, then `seed-phase2-public-evidence -> verify-phase2`: PASS. Verifier reported exact `0002_phase2`, `seed_state=true`, `raw_integrity=true`, 2 datasets and no captured runs/snapshots.

### Validation status / Remaining issue

These are Terra fixture/SQLite corrections only. Phase 2 remains **REVIEW_REQUIRED** until Sol independently reproduces the fresh-DB, parser, verifier/startup and publication-boundary checks. No live call occurred; permission, provider, timestamp/PIT, model/OOS/shadow, Risk Budget and Production statuses remain respectively `NOT VERIFIED`, `BLOCKED_POLICY` where live capture is required, `RISK BUDGET NOT APPROVED` and `NOT PRODUCTION READY`.

## 2026-09-09 exact trusted-declaration gate correction

### Changed / Why

- `optivest/phase2.py`: `trusted_phase2_declarations()` now defines every frozen provider, license, dataset and seven-use permission value used by both seed and verifier. Verification now compares provider authority tier/version/declarations/source reference/validation status; license artifact hash/source URL/retrieval instant/reviewer/reason; dataset linkage/name/semantic version/endpoint/parser name and hash/timestamp semantics/rate policy/validation state; and every permission status and duty exactly.
- `tests/test_phase2.py`: raw-SQL tampering regressions cover authority tier, license artifact hash, license reviewer, dataset rate policy, permission status and permission duty. Each proves `seed_state=false`, nonzero `verify-phase2` CLI exit and app-startup rejection.

### Evidence / Tests / Results

- Focused Phase 2: `uv run pytest tests\\test_phase2.py -q` — **50 passed, 1 skipped, 2 upstream warnings**.
- Full suite: `uv run pytest -q` — **70 passed, 2 skipped, 2 upstream warnings**. The skipped Windows symlink fixtures remain host-permission limitations, not passed coverage.
- Compile/whitespace: `uv run python -m py_compile optivest\\phase2.py optivest\\app.py optivest\\cli.py tests\\test_phase2.py alembic\\versions\\0002_phase2.py`; `git diff --check` — PASS.
- Fresh SQLite migration round trip `upgrade head -> downgrade 0001_phase1 -> upgrade head`, then CLI seed/verify — exact `0002_phase2`, `seed_state=true`, `raw_integrity=true`, two datasets and zero capture records.

### Validation status / Remaining issue

This remains Terra fixture/SQLite evidence only. Phase 2 is **REVIEW_REQUIRED** pending independent Sol revalidation. No live call, permission elevation or Policy/ADR change occurred. Provider/license/timestamp/PIT, model/OOS/shadow and Production remain `NOT VERIFIED`; live capture remains `BLOCKED_POLICY`; `RISK BUDGET NOT APPROVED` and `NOT PRODUCTION READY` remain unchanged.

## 2026-09-09 UNVERIFIED_USER_DIRECTED_CAPTURE Nasdaq trailer parser repair

### Changed / Why

- `optivest/phase2.py`: directory trailers are now parsed as pipe-delimited records: their first field must be the unchanged `File Creation Time` value, and all schema-required remaining fields must be empty. Nasdaq-listed uses seven empty trailer fields; observed otherlisted uses six. The persisted `source_time_text` excludes delimiter padding. An observed `Exchange=M` test row is accepted as known source syntax but remains `REVIEW_REQUIRED` because only A/N/P can be eligible.
- `tests/test_phase2.py`: exact listed/otherlisted trailer regressions reject nonempty trailing data; an observed `M` row regression preserves the review boundary.

### Evidence / Tests / Results

- Offline local files only: `nasdaqlisted.txt` SHA-256 `31A17FF730E8E42753E45051AC44732A3790FF36ED0A03ADF32F0170A211A2E4`, 5,592 rows: 3,002 `ELIGIBLE_FOR_REVIEW`, 2,590 `REVIEW_REQUIRED`; `otherlisted.txt` SHA-256 `4F5AE4F711AFD0E39A75F07393C84C8F549DD98CF58A76459F0DD26A9ED02CE6`, 7,597 rows: 1,732 `ELIGIBLE_FOR_REVIEW`, 5,865 `REVIEW_REQUIRED`. Both retain `0908202621:31` as source timestamp text.
- Status: `UNVERIFIED_USER_DIRECTED_CAPTURE / NOT VERIFIED`. The parent performed the user-directed HTTP retrieval and recorded exact URLs, HTTP 200, request/response instants, byte lengths, line counts, hashes and trailers in `data/unverified_nasdaq_capture_2026-09-09/CAPTURE_MANIFEST.md`. The files were not inserted into the trusted Phase 2 DB. Publisher permission, source-time semantics/PIT, data completeness, security identity, historical validity and Production readiness remain unverified.
- Tests: focused `tests\\test_phase2.py` 52 passed / 1 skipped; remaining `tests\\test_phase1.py` 8 passed and `tests\\test_verify_startup.py` 12 passed / 1 skipped (72 passed / 2 skipped over all 74 tests); compile and `git diff --check` PASS. Skips remain host-denied Windows symlink tests.

### Next action

The independent Sol parser review passed this narrow compatibility repair: both exact hashes/counts reproduced, malformed trailer suffixes were rejected, and `Exchange=M` remained review-only. Provider/license/PIT and Production states remain unchanged.

## RESEARCH-RISK-EDITOR-V1 Terra implementation checkpoint (2026-09-09)

ADR-0017's exact design hash remains the implementation authority. Terra added migration `0003_research_risk_editor`, immutable V1 marker/document records, a strict bounded raw JSON decoder, catalog/template/projection/diagnostic helpers, legacy/V1 successor handling, API/CLI/history/preview routes and a separate `/risk-budget` research page. The V1 service uses `BEGIN IMMEDIATE` and one conditional two-head update for its new save path. Existing provider display permissions and Phase 2 capture behavior were not changed; Phase 2 revision recognition accepts exactly `0002_phase2` or `0003_research_risk_editor`.

Observed checks: fresh isolated SQLite `upgrade -> seed-research -> verify-risk-editor` returned revision `0003_research_risk_editor` and `NOT PRODUCTION READY`; `tests/test_phase1.py` 8 passed; `tests/test_phase2.py` 52 passed, 1 skipped; `tests/test_risk_editor.py` 2 passed; `tests/test_verify_startup.py` 12 passed, 1 skipped; compile and `git diff --check` passed; startup checker PASS with main/UNBORN. These are Terra implementation checks only, not independent acceptance.

Current limitations / next action: Sol must independently falsify the complete ADR-0017 section 11 matrix, particularly exhaustive class/mode grammar, all four legacy/V1 transitions, fault/race injection, populated downgrade/re-upgrade, raw-SQL corruption probes and browser desktop/mobile behavior. No feasibility, impact, calibration, `U_risk`, `S_stress`, provider/PIT, OOS, shadow, personal suitability or Production claim is validated. `RISK BUDGET NOT APPROVED` and `NOT PRODUCTION READY` remain mandatory.

## RESEARCH-RISK-EDITOR-V1 review-fix checkpoint (2026-09-09)

Terra corrected the independent post-review findings: decimal canonicalization preserves integer magnitude; nested risk/stress/hybrid/component/joint validation is closed and catalog-order canonicalized; deterministic unavailable diagnostics and recursive preview changes are emitted; V1 envelope/projection verification is fail-closed; status/preflight surface storage corruption as 503 without snapshot insertion; error responses are top-level and size/media/encoding/SQLite-lock outcomes are classified; CLI dates share API ISO serialization; and `/risk-budget` exposes row/component controls. The independent reviewer suite now reports 29 passed. This is a correction-pass result only; Sol recheck remains required and no approval/readiness state changed.

## RESEARCH-RISK-EDITOR-V1 second bounded correction (2026-09-09)

Following the second Sol recheck, Terra added permanent parameterized preview regressions for the nine remaining forbidden grammar cases. `validate_declaration` now enforces selected risk-mode implication/reason rules, epsilon/alpha bounds, closed stress roles, hybrid participation bounds, nonrecursive unique joint members/bounds, row-16 parent horizon exclusion, component units and `M<string>` typing. The V1 document FK now targets its marker, saves marker before document, verifier reflects required tables/FKs/triggers and validates profile risk chains, and preview returns observed current IDs. The inline editor now reads inputs, handles changes and fetches history. Focused editor plus retained independent tests: 40 passed. No Policy/design/default/permission/status was changed; Sol recheck remains required.

## RESEARCH-RISK-EDITOR-V1 final non-browser Terra correction (2026-09-10)

Terra corrected exact row clocks, currency/text applicability, unique/canonical Joint groups, selected semantics blockers, identity-aware differences, strict V1 envelope/timestamp/projection/legacy verification, trigger-body verification and fail-closed status/preview/preflight. The inline page binds component edits and shows preview diagnostics/history. No Policy, ADR, default value, permission or status changed; browser interaction remains separately unverified.

Evidence: focused editor/reviewer **49 passed**; Phase 1/2 group **60 passed, 1 skipped**; compile/diff/startup PASS; fresh temporary `upgrade -> seed -> verify -> downgrade -> upgrade -> verify` V1 verifier PASS. Monolithic `pytest -q` reached 58% twice but did not complete in the desktop 30-second command window, so it is not claimed as a full pass. Sol recheck/browser workflow remain required; `RISK BUDGET NOT APPROVED` and `NOT PRODUCTION READY` remain unchanged.

## RESEARCH-RISK-EDITOR-V1 S1–S3 correction milestone (2026-09-10)

### Changed / Why

- `optivest/risk_editor.py` now rejects every catalog-invalid row/component clock, wrong currency applicability, blank/non-text declaration metadata, duplicate/reused Joint members and noncanonical Joint ordering. It completes selected risk/stress/hybrid implications and deterministic incomplete diagnostics.
- Declaration differences now retain reason leaves, order by canonical path, use `>=`/minimum/buffer direction correctly, and decline directional claims whenever definition/method/window/confidence/risk/stress identity changed.
- `verify_risk_editor()` now checks exact V1 table checks/FKs, immutable trigger bodies, all profile mandate/risk chains, legacy catalog/class/operator/nonfinite values, and every historical V1's closed envelope/timestamp/document/projection. A coherent but stale mandate/risk pairing remains a preflight blocker rather than storage corruption.
- `/risk-budget` is an independent, safe raw-declaration editor for every frozen field, including row-16 components, M reasons, horizons, risk/stress/hybrid metadata and Joint groups. It supports templates, preview, provisional save, history, explicit stale conflict/reload and `textContent` result rendering. It offers no activation or recommendation.
- `tests/test_risk_editor.py` adds permanent canonical-Joint, difference identity/direction, corrupt legacy catalog and same-name no-op trigger regressions. The retained independent review test file was not edited and its SHA-256 is still `EF9E02011A3B66249B84CFC0BD5654E5A3571CCC314D8E4E21C40143C2AB799B`.

### Evidence / Tests / Results

- Policy hash remains `ACF013CE46F450423D83DEEBA1C207B22C669670C2B9BDC83A8A665F5AD38E67`; design/review/ADR bytes were not changed.
- Editor and retained independent tests: **51 passed, 2 warnings**.
- Phase 1: **8 passed, 2 warnings**. Phase 2: **52 passed, 1 skipped, 2 warnings**; the skip is the existing host-denied Windows symlink limitation.
- Compile, startup checker and `git diff --check` passed. Startup reports main/UNBORN and structural-only `NOT PRODUCTION READY`.
- Full local suite: `uv run pytest -q --tb=line` — **123 passed, 2 skipped, 2 warnings in 49.46s**. The two skips remain host-denied Windows symlink limitations; no skipped coverage is claimed as PASS.

### Validation Status / Remaining Issues / Resume

This is Terra implementation evidence only. `PASS_ENGINEERING_RESEARCH_RISK_EDITOR` must not be set until Sol independently retests the frozen section 11 matrix. Mandatory outstanding acceptance is real local browser desktop/mobile interaction (edit/clear/preview/save/reload/history/stale/XSS and blocked-provider repeat); no available browser session was used in this correction. Feasibility, impact, calibration, `U_risk`, `S_stress`, provider/PIT, OOS, shadow, personal suitability and Production all remain unverified. `RISK BUDGET NOT APPROVED`, `SHADOW VALIDATION NOT PASSED` and `NOT PRODUCTION READY` remain mandatory.

Next agent: perform independent Sol falsification of S1/S2, API/CLI/storage fail-closed handling, and the required browser exercise. Do not edit Policy/design/review/ADR or elevate approval/readiness based on this milestone.
